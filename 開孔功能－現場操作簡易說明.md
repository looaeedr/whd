# 開孔功能－現場操作簡易說明（含截角類型）

## 一、怎麼進入開孔

在主畫面的板件上：

**雙擊板面**

就會開啟「開孔編輯器」。

適用：

* 箱身
* 門板
* 底板
* 指示燈盒
* 指示燈小門
* 封頭
* 封尾

---

## 二、怎麼選孔

左側有兩個清單：

### 一般開孔

例如：

* Ø22
* 方孔
* 插座盒
* 特殊 DXF 孔型

### 管孔清單

例如：

* *D1 Ø116
* *D2 Ø...
* 其他管孔尺寸

操作：

**單擊**
＝選取

**雙擊**
＝直接進入插入模式

也可以先單擊，再按：

**插入**

---

## 三、怎麼放孔

選好孔型後：

1. 到圖面上點一下位置。
2. 孔會出現在板面上。
3. 系統會自動檢查孔有沒有超出合法範圍。

如果正在等你放孔，但不想放了：

**按 Esc**

即可取消。

---

# 四、怎麼定位孔

選中已經放好的孔後：

**在孔上按右鍵**

選擇基準位置。

有 9 種：

```text id="9bqrbj"
左上   中上   右上
中左   中心   中右
左下   中下   右下
```

選完後，圖上會出現十字基準線。

---

# 五、X、Y 尺寸怎麼輸入

定位框會跟著基準線顯示。

## X 定位

會看到：

```text id="o7uxm2"
X 到邊框
X 到鄰近孔
```

## Y 定位

會看到：

```text id="7uv52a"
Y 到邊框
Y 到鄰近孔
```

直接輸入數字即可。

不用再按「套用」。

例如：

```text id="4ch2pp"
X 到左邊框 = 50
```

就是代表：

**成品完成後的左邊到這顆孔基準位置 = 50 mm**

所以：

**50 就是 50**

不用再自己算折彎。

---

# 六、改完孔要按什麼

孔旁邊會有：

```text id="yqojkd"
[確定] [取消]
```

## 確定

保留這次修改。

## 取消

恢復到修改這顆孔之前。

---

# 七、怎麼旋轉孔

上方有：

```text id="d68hr6"
90°
180°
270°
360°
```

特殊孔、方孔、DXF 孔型可以用。

`360°` 就是原始方向。

---

# 八、怎麼改成盲孔

左側「已開孔」清單中：

**雙擊某一顆孔**

就可以切換：

```text id="n3tem5"
空白 ⇄ 盲孔
```

其中：

```text id="lcgjen"
空白 = 一般穿孔
盲孔 = BLIND_HOLE
```

---

# 九、圓孔怎麼排一整排

如果目前選的是圓孔，可以開：

**圓孔排列設定**

裡面可以選方向：

```text id="ggnxyg"
向左
向右
向上
向下
左右兩側
上下兩側
```

---

# 十、孔心距和間距

圓孔排列會同時顯示：

```text id="mg6jsy"
孔心距
間距
```

## 孔心距

兩個圓：

**中心到中心**

的距離。

## 間距

兩個圓：

**圓邊到圓邊最近的距離**

兩個數字會互相跟著變。

你改哪一個，就以你最後改的那一個為主。

---

# 十一、填滿

按：

**填滿**

會保留目前這顆孔的位置，再按照你選的方向繼續增加圓孔。

例如：

```text id="64c2oq"
○ → ○ → ○ → ○ → ○
```

一直排到不能再放為止。

---

# 十二、重新填滿

按：

**重新填滿**

會重新計算整排位置。

不一定保留目前孔原本的位置。

適合：

* 想把整排重新排整齊
* 想盡量利用可用空間
* 原本孔的位置不重要

---

# 十三、圓孔對齊

如果附近另一顆孔也是圓孔，會多出：

```text id="4x46c8"
孔心齊
管頂齊
管底齊
```

## 孔心齊

兩個圓中心對齊。

## 管頂齊

兩個圓最上方對齊。

## 管底齊

兩個圓最下方對齊。

不同大小的管孔也能使用。

---

# 十四、圓孔排列和一般定位衝突怎麼辦

記一個原則：

**最後按哪一邊的「確定」，就以哪一邊為準。**

例如：

先用一般定位設定：

```text id="dnt2kf"
X = 50
```

然後再進圓孔排列按「確定」。

最後就以：

**圓孔排列**

為準。

如果之後又回一般定位修改，再按「確定」：

就以：

**一般定位**

為準。

---

# 十五、做錯了怎麼辦

上方有：

**↶ 回上一步**

也可以按：

**Ctrl + Z**

例如：

```text id="a39rqj"
孔放錯
→ 回上一步

位置改錯
→ 回上一步

盲孔切錯
→ 回上一步

整排孔排錯
→ 回上一步
```

可以連續回很多步。

---

# 十六、Esc 怎麼用

## 正在放孔

**Esc**
＝取消這次放孔

## 正在修改某一顆孔

**Esc**
＝取消這顆孔本次修改

## 沒有正在修改

**Esc**
＝取消整個開孔編輯

---

# 十七、全螢幕

按：

**F11**

可以切換：

```text id="2j7w6f"
全螢幕 ⇄ 一般視窗
```

畫面太小時建議直接開全螢幕。

---

# 十八、最後完成

整個開孔都完成後：

按：

**確定全部**

保存全部修改。

如果這次全部都不想要：

按：

**取消全部**

會回到剛開啟開孔編輯器之前。

---

# 現場快速流程

## 放一顆一般孔

```text id="tpa36s"
雙擊板面
→ 雙擊孔型
→ 點位置
→ 右鍵選基準
→ 輸入 X / Y
→ 確定
```

## 放一顆管孔

```text id="tq1476"
雙擊板面
→ 管孔清單
→ 雙擊管孔
→ 點位置
→ 右鍵選基準
→ 輸入尺寸
→ 確定
```

## 排一整排圓孔

```text id="m5u80u"
先放一顆圓孔
→ 選中
→ 圓孔排列設定
→ 選方向
→ 設孔心距或間距
→ 填滿 / 重新填滿
→ 確定
```

## 做錯

```text id="u0rybl"
Ctrl + Z
```

記住最重要三件事：

**雙擊板面＝開孔**

**右鍵孔＝選基準**

**50 就是成品尺寸的 50**

---

# 附錄：截角類型快速說明

這一段是給現場快速看「截角類型」用。

先記一個原則：

**截角類型、截角方式、方向、T 倍數，是不同層級。**

不要把「多切 1.5T」當成一個新的截角類型。

---

## 一、十字截角 CROSS

```text
十字截角 CROSS
├─ 標準 STANDARD
│  └─ 不額外留肉、不額外多切
│
├─ 單邊留肉 RETAIN
│  ├─ 寬 WIDTH
│  └─ 高 HEIGHT
│  + amount_t 可設定
│
└─ 多切 EXTRA_CUT
   ├─ 寬 WIDTH
   ├─ 高 HEIGHT
   └─ 寬＋高 BOTH
   + amount_t 可設定
```

### 標準 STANDARD

不做額外補償。

### 單邊留肉 RETAIN

可以選：

```text
寬
高
```

再輸入留肉量，例如：

```text
1T
0.5T
```

### 多切 EXTRA_CUT

可以選：

```text
寬
高
寬＋高
```

再輸入多切量，例如：

```text
0.5T
1T
1.5T
```

多切是一種加工方式。

它可以用來：

* 避讓同一片板金自己的折邊
* 避讓另一片板金的板厚
* 避讓另一片板金折起來後的折邊

所以不能只看「自己這片板有沒有同方向折彎」就判定多切有沒有用。

---

## 二、貼外型 OVERLAY

封頭／封尾上方拿來表示貼外組合時：

```text
貼外型 OVERLAY
├─ 單級截角
├─ 留肉方向：高 HEIGHT（固定）
├─ 留肉量：amount_t 可設定
│  └─ 預設 1T
└─ 裝配外高占用：1T
```

### 封頭／封尾選貼外型時

目前已確認：

```text
貼外型 OVERLAY
├─ 左右 X 向折彎：沒有
├─ 2D：不應出現左右 X BEND
├─ 3D：不應出現左右 X BEND
└─ 3D 折彎編輯：不顯示 X 軸折彎頁
```

封頭／封尾的「下方截角」是另一個獨立加工角。

明確選擇貼外型時，下方目前預設帶入：

```text
下方截角
└─ 十字截角 CROSS
   └─ 多切 EXTRA_CUT
      └─ 寬 WIDTH
         └─ 1.5T
```

這只是**預設值**。

之後仍然可以手動改成：

```text
高 HEIGHT
寬＋高 BOTH
其他 T 倍數
或其他既有截角設定
```

---

## 三、嵌入型 INSERT

封頭／封尾上方拿來表示純嵌入組合時：

```text
嵌入型 INSERT
├─ 單級截角
├─ 不留肉
├─ 多切方向：高 HEIGHT（固定）
├─ 多切量：amount_t 可設定
│  └─ 通常預設 1T
└─ 裝配外高占用：0T
```

意思就是封頭／封尾嵌入箱身內，不再另外占外部高度的 1T。

---

## 四、嵌入貼外型 INSERT_OVERLAY

這個型態同時有嵌入和貼外兩個作用，所以是二級截角。

```text
嵌入貼外型 INSERT_OVERLAY
├─ 第一級：貼外留肉
│  ├─ 方向：高 HEIGHT（固定）
│  └─ 貼外留肉：預設 1T
│
├─ 第二級：嵌入留肉
│  └─ 預設 0.5T
│
├─ 第二級深度
│  └─ 預設 2T
│
└─ 裝配外高占用：1T
```

第二級的位置不是直接用 `FW - 0.5T` 當切線位置。

程式使用的關係是：

```text
第二級 CUTTING
= 側折 + 嵌入留肉量
```

例如：

```text
T = 2 mm
嵌入留肉 = 0.5T = 1 mm
側折 = 15 mm
```

第二級 CUTTING 位置就是：

```text
15 + 1 = 16 mm
```

---

# T 倍數怎麼看

`T` 就是板厚。

例如：

```text
板厚 T = 2 mm
```

那麼：

```text
0.5T = 1 mm
1T   = 2 mm
1.5T = 3 mm
2T   = 4 mm
```

現場輸入的是 T 倍數時，系統會依目前板厚換算。

---

# 封頭／封尾要特別注意

封頭／封尾有兩種不同責任的截角：

```text
上方截角
→ 表示與箱身的組合／裝配關係
→ INSERT / OVERLAY / INSERT_OVERLAY

下方截角
→ 加工避讓用
→ 可以使用 CROSS 的標準／留肉／多切
```

**不要把上方裝配截角和下方加工截角當成同一個東西。**

---

# 舊截角代碼對照

舊檔可能還會看到：

```text
C01
C02
C03
C04
```

對照如下：

```text
C01
→ 十字截角 CROSS
   └─ 標準 STANDARD

C02
→ 十字截角 CROSS
   └─ 單邊留肉 RETAIN
      └─ 1T

C03
→ 十字截角 CROSS
   └─ 多切 EXTRA_CUT
      └─ 寬＋高 BOTH
         └─ 0.5T

C04
→ 嵌入貼外型 INSERT_OVERLAY
   ├─ 貼外留肉 1T
   ├─ 嵌入留肉 0.5T
   └─ 深度 2T
```

新介面操作時，以中文截角名稱為主，不需要再選 C01～C04。

---

# 截角現場快速記法

```text
十字截角
├─ 標準
├─ 留肉 → 寬 / 高 → xT
└─ 多切 → 寬 / 高 / 寬+高 → xT

貼外型
→ 上方裝配用
→ 封頭尾無左右 X 折彎
→ 明確選貼外時，下方預設：十字多切／寬／1.5T

嵌入型
→ 上方裝配用
→ 高方向多切 xT
→ 外高占用 0T

嵌入貼外型
→ 上方裝配用
→ 貼外留肉 + 嵌入留肉 + 二級深度
→ 外高占用 1T
```

