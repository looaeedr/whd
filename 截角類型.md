# WHD 鈑金展開工具 — 使用說明書

## 截角類型

```text
截角類型
├─ 十字截角 CROSS
│  ├─ 標準 STANDARD
│  │  └─ 不額外留肉、不額外多切
│  │
│  ├─ 單邊留肉 RETAIN
│  │  ├─ 寬 WIDTH
│  │  └─ 高 HEIGHT
│  │  + amount_t 可設定
│  │
│  └─ 多切 EXTRA_CUT
│     ├─ 寬 WIDTH
│     ├─ 高 HEIGHT
│     └─ 寬＋高 BOTH
│     + amount_t 可設定
│
├─ 貼外型 OVERLAY
│  └─ 高 HEIGHT
│  + amount_t 可設定
│
├─ 嵌入型 INSERT
│  └─ 高 HEIGHT
│  + amount_t 可設定
│
└─ 嵌入貼外型 INSERT_OVERLAY
   ├─ 高 HEIGHT
   │  + amount_t 可設定
   ├─ 嵌入留肉
   │  + secondary_retain_t 可設定
   └─ 嵌入深度
      + secondary_depth_t 可設定
```

## 2026-08-29 補充：上方裝配截角的「類型」與「實際尺寸」分離

- `INSERT / OVERLAY / INSERT_OVERLAY` 描述 assembly intent / 合法截角 topology，不代表可以用固定公式當最終尺寸。
- 封頭／封尾上方真正 CUTTING 尺寸由折後 3D 裝配干涉求解；正常 skin contact 不得算成 penetration。
- 單級 `INSERT` 必須維持單級 topology。`自訂(9)` 的 `38×27` 是該組 W/H/D/T/FW/Fold Profile 的實際 regression 結果，不是 INSERT 的固定尺寸。
- 顯示尺寸必須量 canonical final material；2D、單板 3D、組合圖不可各自用 CornerType residual／legacy fixed relief 重算。

## 2026-08-29 補充：所有 Assembly Intent 共用的 3D 驗收規則
- `OVERLAY` 也是 3D collision solver 的正式 assembly intent，不得因 X 向沒有折彎就跳過 collision / topology 驗證。
- 類型只定義合法 topology：`OVERLAY` / `INSERT` 為單級語意時，solver 不得因 triangle crossing 雜訊長出 0.x mm 假二級；`INSERT_OVERLAY` 的真正二級則必須保留。
- 對稱組合若 Box Body X、EndCap X、corner component 都通過鏡像幾何判定，左右最終 relief 必須一致；非對稱 profile 不套此規則。
- 新增新的 Assembly Intent 時，必須加入 `BOX_ASSEMBLY_TYPE_IDS` registry，並自動通過 registry-driven Head/Tail / collision / topology / 2D-3D / Save-Reload 測試後才能視為可用。

## 2026-08-29 補充：已認證截角資料庫優先
- 前段舊說明「封頭／封尾上方真正 CUTTING 尺寸由 3D 裝配干涉求解」已被新架構限縮：**只有 registry MISS 的未知組合才由 3D discovery 決定 provisional 尺寸**。
- 已知組合命中 `CERTIFIED / CERTIFIED_FROM_3D` 時，資料庫中的參數化公式是正式製造真值；3D 只能 shadow validation，不能改公式。
- `ENGINE_CONFLICT` 表示 3D 與已認證公式衝突；正式結果仍採 certified rule，並要求診斷 3D model / transform / precondition。
- `REGISTRY_AMBIGUOUS` 禁止任意選規則或偷偷 fallback。
- `自訂(9)` 38×27 與 `自訂(10)` 40×23 + 16×4 都是 fixture 證據，不是資料庫死尺寸；正式 rule 由 Fold/FW/T 公式推導。
- fixed CornerType（Vault/受電箱的 Head/Tail/Door/Base/Indicator）也由同一 family-aware registry 供應。

## OVERLAY flat-X 的名義側折規則（2026-08-29 修正）

`OVERLAY` 沒有左右 X 向折彎，但「沒有折彎」只影響 BEND topology，不會把截角公式所需的名義側折尺寸清成 0。

標準上方貼外型單級截角：

```text
primary_u = nominal_side_fold + FW
primary_v = ytop1 + FW - amount_t*T
secondary = None
```

以 `nominal_side_fold=15, FW=25, ytop1=16, T=2, amount_t=1`：

```text
40 × 39
```

舊版曾因 flat-X 將 `side_fold` 錯設為 0 而得到 `25×39`；該值已判定為錯誤，不得再當認證公式或回歸真值。
