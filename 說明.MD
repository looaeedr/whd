
# ==========================================
# 全域變數定義區 (大寫標準簡寫)
# ==========================================
W = 400.0   # 寬 (Width)
H = 600.0   # 高 (Height)
D = 250.0   # 深 (Depth)
T = 1.5     # 板厚 (Thickness)
FW = 15.0   # 邊框寬 (Frame Width)


# ==========================================
# 箱身 (z) 公式定義區
# ==========================================
def calculate_z_length(zl1=15.0, zl2=20.0, zr1=15.0, zr2=20.0, z_comp=1.2):
    """
    計算【箱身 z】總料長度
    
    預設參數:
    - zl1 = 15.0, zr1 = 15.0
    - zl2 = 20.0, zr2 = 20.0
    - z_comp : 箱身料補償
    
    原始邏輯: zl1 + zl2 + FW + (D - 2T) + (W - 2T) + (D - 2T) + zr2 + zr1 + z_comp
    化簡公式: zl1 + zl2 + zr1 + zr2 + FW + W + 2*D - 6*T + z_comp
    """
    total_length = (
        zl1 + 
        zl2 + 
        zr1 + 
        zr2 + 
        FW + 
        W + 
        (2 * D) - 
        (6 * T) + 
        z_comp
    )
    return total_length


# ==========================================
# 封頭尾 (y) 公式定義區
# ==========================================
def calculate_y_width(yl1=15.0, yr1=15.0):
    """
    計算【封頭尾 y - 寬度方向】展開長度
    
    預設參數:
    - yl1 = 15.0 (左折)
    - yr1 = 15.0 (右折)
    
    公式: W - 4*T + yl1 + yr1
    """
    total_width = W - (4 * T) + yl1 + yr1
    return total_width


def calculate_y_depth(ytop1=15.0, ybottom1=15.0):
    """
    計算【封頭尾 y - 深度方向】展開長度
    
    預設參數:
    - ytop1 = 15.0 (上折1)
    - ybottom1 = 15.0 (下折1)
    
    公式: D - 3*T + ytop1 + FW + ybottom1
    """
    
    total_depth = D - (3 * T) + ytop1 + FW + ybottom1
    return total_depth

# ==========================================
# 門 (door) 公式定義區
# ==========================================
def calculate_door_finished_size(gap_w=3.5, gap_h=3.5):
    """
    計算【門】成品尺寸
    - 成品邊框寬度為 FW + 2T
    
    公式:
    - 成品寬 = W - (FW + 2T)*2 - gap_w*2
    - 成品高 = H - (FW + 2T)*2 - gap_h*2
    """
    finished_w = W - (FW + 2 * T) * 2 - gap_w * 2
    finished_h = H - (FW + 2 * T) * 2 - gap_h * 2
    return finished_w, finished_h


def calculate_door_blank_size(gap_w=3.5, gap_h=3.5, fl=19.0, fr=15.0, ft=15.0, fb=15.0):
    """
    計算【門】展開下料尺寸 (Flat Pattern)
    - 門寬方向上下折邊 (ft/fb) 兩側要留肉延伸 T (板厚) 包住左右折
    - 折彎線不作延伸，維持原長度 (fl 到 blank_w - fr)
    
    公式:
    - 門寬總料 = 成品寬 - 2T + 左折 + 右折
    - 門高總料 = 成品高 - 2T + 上折 + 下折
    """
    finished_w, finished_h = calculate_door_finished_size(gap_w, gap_h)
    blank_w = finished_w - 2 * T + fl + fr
    blank_h = finished_h - 2 * T + ft + fb
    return blank_w, blank_h


# ==========================================
# 指示燈盒子 (indicator_box) 公式定義區
# ==========================================
#
# 【結構說明】
#   - 直的 3 個指示燈為「一組」，一組就是一層。
#   - 橫向可有多「組」(columns)，縱向可有多「層」(layers)。
#   - 第 1 層為最上面那一層 (最頂)，第 N 層為最下面 (最底)。
#   - 每一層可以獨立設定其橫向組數 (columns)，彼此無關。
#   - 層與層之間相鄰指示燈的中心距離為 100 mm。
#
# 【展開尺寸公式】
#   g = layer_groups: 各層組數的列表，例如 [3, 2, 2] 代表：
#                     第 1 層 (頂) 3 組、第 2 層 2 組、第 3 層 2 組
#   g_max = max(g)  : 所有層中最大的組數
#
#   展開寬  W = 326.0 mm (當 g_max=1 時)；否則 W = 326.0 (當 g_max=1 時)；否則 W = 171 + 90 * (g_max - 1) + 135 mm
#   展開高  H = 280 * (L - 1) + 445            mm   (L = 層數)
#
# 【折邊】
#   上下左右各折 49 mm，缺角為 47 × 49 mm
#
# 【指示燈孔位置】
#   每一層 ly (0=頂, L-1=底)，每一組 i：
#   - 指示燈中心 X  = 191.0 (當 g_max=1 時)；否則 X = 171.0 + 90.0 * i
#   - 層起點    Y_start = 133.5 + 280.0 * (L - 1 - ly)
#   - 三顆燈 Y 座標   = Y_start, Y_start+90, Y_start+180
#
# 【名牌安裝孔】
#   每一層每一組的最上方那一顆指示燈 (Y = Y_start + 180) 上方 48mm 處
#   各有一對名牌安裝孔 (半徑 1.6，間距 44，左右偏移 ±22)：
#   名牌孔 Y = Y_start + 180 + 48 = Y_start + 228
#
# 【線槽打標孔】
#   X 軸：當 g_max=1 時，X = 191.0；否則統一以所有層中組數最多的 g_max 計算居中對稱，50 的整數倍孔距
#   Y 軸：每一層 ly 的線槽打標孔 Y = 178.5 + 280 * (L - 1 - ly)
#          → 第 1 層 (頂，ly=0)：Y = 178.5 + 280*(L-1)
#          → 最底層 (ly=L-1)  ：Y = 178.5
#   孔數：g_max ≤ 1 → 1孔；g_max ≤ 3 → 2孔；g_max ≤ 5 → 3孔；g_max > 5 → 4孔

def calculate_indicator_box_size(layer_groups):
    """
    計算【指示燈盒子】展開下料尺寸
    
    layer_groups: list[int]，例如 [3, 2] → 第 1 層 3 組、第 2 層 2 組
    
    公式:
    - g_max = max(layer_groups)
    - 展開寬 W = 326.0 (當 g_max=1 時)；否則 W = 171 + 90 * (g_max - 1) + 135
    - 展開高 H = 280 * (L - 1) + 445      (L = len(layer_groups))
    """
    L = len(layer_groups)
    g_max = max(layer_groups) if layer_groups else 1
    blank_w = 171 + 90 * (g_max - 1) + 135
    blank_h = 280 * (L - 1) + 445
    return blank_w, blank_h


# ==========================================
# 主程式執行區
# ==========================================
if __name__ == "__main__":
    
    # 1. 計算箱身 z (直接套用預設值，僅傳入料補償)
    result_z = calculate_z_length(z_comp=1.2)
    
    # 2. 計算封頭尾 y 的寬與深 (全部套用預設值與全域變數)
    result_y_width = calculate_y_width()
    result_y_depth = calculate_y_depth()

    # 3. 計算門成品與下料尺寸
    door_f_w, door_f_h = calculate_door_finished_size()
    door_b_w, door_b_h = calculate_door_blank_size()

    # 4. 計算指示燈盒子尺寸 (預設 1 層 2 組)
    layer_groups_example = [2]   # 1 層 2 組
    ib_w, ib_h = calculate_indicator_box_size(layer_groups_example)

    # --- 輸出結果 ---
    print(f"【目前全域設定】")
    print(f"-> W(寬): {W}, H(高): {H}, D(深): {D}, T(板厚): {T}, FW(邊框): {FW}")
    print("-" * 50)
    print(f"【箱身 z】總料長度: {result_z} mm")
    print(f"【封頭尾 y】下料尺寸: {result_y_width} mm (寬) x {result_y_depth} mm (深)")
    print(f"【門成品】尺寸: {door_f_w} mm (寬) x {door_f_h} mm (高) [邊框為 FW+2T={FW+2*T} mm]")
    print(f"【門展開】下料尺寸: {door_b_w} mm (寬) x {door_b_h} mm (高)")
    print(f"【指示燈盒子】展開尺寸 (1層2組): {ib_w} mm (寬) x {ib_h} mm (高) [上下左右各折 49 mm]")