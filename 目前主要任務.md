
## 2026-08-29 當前已完成／下一步基準

### 已完成
- `自訂(9).p6fold` 封頭／封尾單級 INSERT 上方 relief 已校正為真實 **38×27**。
- 2D、單板 3D、組合圖使用同一 canonical `PartRenderData.material`；實檔 Head/Tail symmetric difference = 0。
- 組合圖左側可顯示各板件截角尺寸、可控制板件是否顯示；各板件 2D/3D 顯示自己的截角尺寸；全螢幕旁有「回2D截角」。
- solver 對單級 INSERT 排除正常 ±T/2 skin contact band，不再把 38 誤算成 38.98/39；單板 3D 不得回退到 40×27 legacy fixed relief。

### 下一步禁止回退
- 不得再建立第二套 2D/3D 截角尺寸來源。
- 不得硬寫 38；任何新尺寸都必須由 3D mating geometry 動態求解並 refold verified。
- 新 Bugfix/功能出包時，文件與程式必須 atomic 同版交付。

---

現在最適合的是做漸進式改造，不重寫 AE。

你現在已經有能工作的 GUI AE，也有能工作的拆圖 AE。先把「兩邊之間那條接口」固定，之後才做到你要的：

AE 更新 → 直接覆蓋核心資料夾，不再人工合併。

你現有文件其實已經把最重要的契約定好了：拆圖只做到 finished-face Feature，展開座標與加工圖由 AE 負責。 而且 finalized info_data 已經是拆圖輸出 AE 的唯一 authoritative source，這也不用推翻。

第一階段：先新增一個穩定 API，不搬任何演算法

先在現在 AE 專案新增：

manufacturing_api.py

外界以後只准從這裡進。

例如先定成：

job = CabinetJob(
    width=500,
    height=600,
    depth=200,
    thickness=2.0,
    model="金庫型",
    parts=[...],
)


result = generate_cabinet(job)

或者單零件：

result = generate_part(
    PartSpec(
        kind="door",
        width=500,
        height=600,
        features=[...],
    )
)

第一版這個 API 裡甚至可以只是包裝你現在的：

export_door_dxf()
export_box_body_dxf()
export_end_cap_dxf()

先不要重寫它們。

目的只有一個：

GUI 與拆圖以後不准直接碰底下 exporter。

第二階段：固定三個資料契約

我會只定三個核心物件：

CabinetSpec
PartSpec
Feature

其中 Feature 就沿用你現在已經驗證過的語意：

Feature(
    surface="door",
    x=120.0,
    y=300.0,
    ...
)

x/y 永遠是：

finished-face 上的 1:1 mm 實體座標。

這正是目前文件已明定的契約。

所以不重新發明座標系。

第三階段：把 automatic_features 這種來源名稱消掉

現在像：

export_end_cap_dxf(
    ...,
    holes=gui_holes,
    automatic_features=auto_features,
)

這就是目前需要人工合併的原因之一，因為 AE 開始知道「拆圖」存在。文件也特別提醒這個 interface 不能被 upstream 覆蓋。

以後改成：

PartSpec(
    kind="end_cap",
    features=[
        ...所有 Feature...
    ]
)

核心不問：

這是 GUI 來的？
還是自動拆圖？

都是 Feature。

來源 ownership 留在 Adapter。

第四階段：GUI 變 Adapter

現在 GUI 有：

self.w_var
self.h_var
self.d_var
surface_features
door_layout
...

先不刪。

只新增：

def build_job_from_gui(self):
    return CabinetJob(...)

然後 GUI 的「出圖」從：

GUI
→ 各種 export_xxx

變：

GUI
→ build_job_from_gui()
→ manufacturing_api.generate_cabinet()

只要做到這一步，就已經證明 API 能涵蓋你目前整套：

多門
箱身三面
封頭尾
指示燈盒
小門
基準檔
MARKING
BLIND_HOLE

而且 GUI 外觀完全不用改。

第五階段：拆圖也只做一個 Adapter

拆圖端新增一個很薄的東西，例如：

modules/
    ae_adapter.py

它只負責：

finalized info_data
+
automatic finished-face Features
        ↓
CabinetJob / PartSpec

例如：

job = build_job_from_split_result(
    info_data=row,
    features=features,
)

接著：

manufacturing_api.generate_cabinet(job)

拆圖原本的這些全部繼續留著：

DXF ownership
physical feature grouping
開孔.csv
自動開孔替換.csv
selector
UNMATCHED_SOURCE
PANEL 判斷
底座判斷

因為那本來就是拆圖責任。文件也是這樣劃分。

然後才做你真正想要的「直接覆蓋」

等 GUI、拆圖都只從 API 進之後，再把核心整理成一個完整 package：

ae_engine/
├─ __init__.py
├─ api.py
├─ contracts.py
├─ geometry.py
├─ features.py
├─ part_adapters.py
├─ drawing.py
├─ exporters.py
└─ factory_policy.py

而且 ae_engine 裡全部使用相對 import：

from .geometry import ...
from .features import ...

不要再有：

from modules.sheetmetal_geometry ...

也不要有：

from sheetmetal_geometry ...

這很關鍵。

這樣整個：

ae_engine/

不管放：

AE專案/ae_engine/

還是：

拆圖專案/modules/ae_engine/

內容都完全一樣。

資源路徑也要一起斷開

現在文件花很多篇幅保護 get_resource_path()，因為拆圖版 ae.py 在 modules，基準檔卻在專案根目錄。

新 API 不要讓 Core 猜。

直接傳：

context = ManufacturingContext(
    resource_root=r"Z:\CAD\基準檔",
    output_root=r"Z:\輸出\加工圖",
)

所以：

AE GUI
→ resource_root = AE專案\基準檔


拆圖
→ resource_root = 拆圖專案\基準檔

核心完全不在乎自己被放在哪裡。

這樣 get_resource_path() 那堆特殊相容邏輯最後都可以退休。

最重要：不要現在把 ae.py 拆掉

我會照這個順序：

① 定 contracts.py
        ↓
② 做 manufacturing_api.py
   包住現有 AE，不改演算法
        ↓
③ GUI 改成只呼叫 API
        ↓
④ 真 DXF 全部 regression
        ↓
⑤ 拆圖 adapter 改成只呼叫 API
        ↓
⑥ 真拆圖 regression
        ↓
⑦ 確認 GUI / 拆圖結果一致
        ↓
⑧ 才整理成 ae_engine package
        ↓
⑨ 之後 ae_engine 可以整包直接覆蓋
我會先做一個很重要的驗證

同一筆資料：

W/H/D
T
model
Features

分別經過：

GUI Adapter → API
拆圖 Adapter → API

最後產生的 PartSpec 必須完全相同。

然後 DXF 也應該完全一致。

這個測試一旦建立，以後你更新 AE：

直接覆蓋 ae_engine/
→ pytest
→ 真 DXF regression

通過就完事。

不用再拿那份《整合出圖合併注意事項》一條一條人工合併。

所以現在第一刀，我建議只做 contracts.py + manufacturing_api.py，完全不動 GUI 畫面、不動拆圖辨識、不動幾何算法。

先證明目前的 Door / Box Body / End Cap 能透過無 GUI API 完整出圖。成功之後，再把 GUI 與拆圖接過去。這樣最安全。
## 2026-08-29 已完成：Assembly Intent 自動驗收閘門
- 目前所有 `BOX_ASSEMBLY_TYPE_IDS` 都由 registry matrix 自動測試，不再手工維護組合白名單。
- 必過 invariant：Head/Tail pre-solve collision、post-solve verified、合法 corner topology、對稱幾何 mirror equality、2D=single3D=assembly、collision overlay、Save/Reload。
- 新增組合方式若只加 production registry、沒通過這個 matrix，視為未完成，不得交付。

## 2026-08-29 已認證截角資料庫
目前主線已改為 **Certified-first / 3D-fallback / 3D-shadow-validation**。

已完成：
- 金庫型／受電箱已知固定 Corner policy registry 化。
- INSERT / OVERLAY / INSERT_OVERLAY 標準公式 registry 化。
- linked-FW INSERT 與 linked-FW INSERT_OVERLAY 認證公式。
- family-aware lookup、ambiguity gate、revision persistence、promotion candidate、registry-driven GUI matrix。

後續新增任何新組合：先 registry MISS → 3D provisional → 多參數驗證 → 整理公式 → 人工核准 → `CERTIFIED_FROM_3D`；未升格前不得污染既有 certified rules。

### 已完成：INSERT 單級拓撲 / linked-FW C04 錯誤認證修正（2026-08-29）
- [x] INSERT 清除 secondary_* 非法殘留。
- [x] Registry topology_levels 強制驗證。
- [x] linked-FW INSERT_OVERLAY 恢復既有 C04 公式，fixture 40×23 + 16×4。
- [x] 自訂(10) 原 INSERT_OVERLAY 與切換純 INSERT 均實檔 Save/Reload 驗證。

### 2026-08-29 OVERLAY 名義側折修正 — 已完成
- [x] 找出 `25×39` 根因：flat-X 的有效 BEND=0 被錯當成 relief nominal side=0。
- [x] 2D structural builder 分離有效折彎與名義截角基底。
- [x] Certified Registry 使用 PartRenderData 保存的 nominal left/right fold。
- [x] 標準 OVERLAY fixture 修正為 40×39 單級。
- [x] 真 GUI Head/Tail / 2D / assembly 一致。
- [x] INSERT 38×27 與 INSERT_OVERLAY 40×23+16×4 無退化。
