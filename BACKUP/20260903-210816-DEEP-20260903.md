# 2026-09-03 修改日誌

## GUI / 3D 效能知識固化

- 新增 `.agents/skills/engineering/phase6-gui-performance-integrity/SKILL.md`，固定 Scheduler 唯一 calculation executor、Transaction Guard 職責、GUI↔3D anti-echo、revision/transaction_id、set-if-changed、3D atomic initialization、DXF source/derived cache 分層、網路磁碟 `SOURCE_UNVERIFIED`、Save/Export 強制 flush、真 GUI 壓力 gate 與 durable checkpoint。
- `.agents/skills/skill_registry.json` 新增 `phase6-gui-performance` route；GUI 效能、live-sync、重算、DXF cache、debounce 與 `gui.py/fold_designer_bridge.py` 修改會自動要求 `phase6-gui-performance-integrity`。
- `AGENTS.md` 的 Skill Preflight 最低要求同步新增 GUI 效能 route。
- 全域踩坑庫新增 #77～#83：live-sync 重算風暴、DXF cache 分層、3D initialization revision、durable checkpoint、pytest cache 污染 pristine overlay、真 GUI 壓力 gate、release runner process-group cleanup race。
- `phase6-release-packaging` Skill 補強 durable checkpoint、父程序先退出仍需確認 process group cleanup、pristine FULL/UPDATE overlay 比對規則。
- 新增 `tests/test_phase6_gui_performance_knowledge.py`，以 regression 鎖住 registry/Skill/踩坑庫內容。
- `release_required_artifacts.json` 將新 Skill、知識 regression 與本日修改日誌列為 mandatory update files，避免後續 UPDATE 漏包。

## 不變項

- 本輪只修改 AI/流程知識、registry、測試與文件；未修改 Phase6 production geometry/runtime behavior。
- `config.ini` 不修改。

## 深模組效能架構深化實作／Release Runner 防錯

- 完成 Scheduler 唯一 calculation executor、GUI↔3D anti-echo/atomic init、derived cache owner、`SOURCE_UNVERIFIED` strict manufacturing gate，以及 Manufacturing → Collision 單向 contract 的 T01～T12 targeted/stress 收口。
- release 全回歸期間抓到並修正 `tools/phase6_release_test_runner.py --reset` 的 lock ordering：reset 現在先取得 journal run lock 才能刪除 journal/state；並以真 concurrent CLI regression 鎖住「持鎖時第二個 reset 不得改動原 journal」。
- 修正 legacy `SimpleNamespace` GUI handler fixture 未實作 `_request_phase6_update` 的測試契約落差；production 不新增 direct calculation fallback，保持 Scheduler 唯一 executor。
- 全域踩坑庫新增 #84～#85，並同步補強 `phase6-release-packaging` 與 `phase6-gui-performance-integrity` Skill。
- `config.ini` 保持不修改；本輪不改 Certified Registry、STANDARD、AssemblyJoint、INSERT／OVERLAY／INSERT_OVERLAY／包覆貼外等機械真值。

## 2026-09-03 20:31+08:00 — Release canonical baseline 更新
- Phase6 cumulative UPDATE canonical baseline 改為 `PHASE6_GUI_PERFORMANCE_KNOWLEDGE_FULL_20260903_124853.zip`。
- `PHASE6_FW_LINK_BUGFIX_FULL_20260823_212355(3).zip` 降為歷史 release 紀錄，不再是現行 packaging gate。
- 因 124853 baseline 已不存在舊 `skills/` 與 retired `tests/test_phase6_user_p6fold_live_sync.py`，現行 `update_cleanup_paths` 清空；未來只有新的刪除/搬移才新增 cleanup。
- `config.ini` 仍禁止進 UPDATE，且內容不得因此次 policy 遷移而修改。
