# WHD 鈑金展開工具 — 使用說明書

## 截角類型

```text
截角類型
├─ 十字截角 CROSS
│  ├─ 標準 STANDARD
│  │  └─ 不額外留肉、不額外多切
│  │
│  ├─ 單邊留肉 RETAIN
│  │  ├─ 寬 WIDTH
│  │  └─ 高 HEIGHT
│  │  + amount_t 可設定
│  │
│  └─ 多切 EXTRA_CUT
│     ├─ 寬 WIDTH
│     ├─ 高 HEIGHT
│     └─ 寬＋高 BOTH
│     + amount_t 可設定
│
├─ 貼外型 OVERLAY
│  └─ 高 HEIGHT
│  + amount_t 可設定
│
├─ 嵌入型 INSERT
│  └─ 高 HEIGHT
│  + amount_t 可設定
│
└─ 嵌入貼外型 INSERT_OVERLAY
   ├─ 高 HEIGHT
   │  + amount_t 可設定
   ├─ 嵌入留肉
   │  + secondary_retain_t 可設定
   └─ 嵌入深度
      + secondary_depth_t 可設定
```


## 專案讀寫

```text
主視窗左上角／3D 視窗左上角
├─ 開啟專案
├─ 儲存專案
└─ 另存新檔
```

- 3D 開啟時主視窗不能操作，所以 3D 左上仍可直接做全域專案讀寫。
- 3D 底部的 `確定 / 取消` 只處理目前板件，不是全專案存檔。
- EXE 完成 `.p6fold` 關聯後，也可直接雙擊 `.p6fold` 開啟專案。

## 截角參數鎖

```text
截角類型／目前摘要
└─ 🔒 參數鎖定
   ├─ 細部參數隱藏
   ├─ 左右相同等進階控制隱藏
   └─ 🔓 參數解鎖
      └─ 需要時才展開修改
```

- 鎖定時隱藏的是細部參數，不留空白佔位。
- 已知型號：截角類型固定，解鎖後可改同類型的細參數。
- 指示燈盒／指示燈小門：固定唯讀，不能解鎖。

## 3D 草稿、確定、取消與專案存檔

```text
主 GUI 已確認資料（committed）
        │
        ├─ 開啟 3D → 建立隔離草稿（draft）
        │              ├─ 取消 / X → 丟棄草稿，主 GUI 不變
        │              └─ 確定 → 套回主 GUI，再成為新的 committed
        │
        └─ 儲存專案 / 另存新檔 → 永遠保存 committed 機械資料
```

- 3D 尚未按「確定」時，即使已修改尺寸、截角、Fold Profile、開孔或板件增刪，全域「儲存專案／另存新檔」都**不會**把這些未確認內容寫進 `.p6fold`。
- 3D 的「取消」或直接關閉視窗會丟棄本次草稿；主 GUI 與正式專案資料維持開啟 3D 前的 committed 狀態。
- 3D 按「確定」後，修改會先套回主 GUI，再成為新的 committed；之後全域存檔才會保存新內容。
- 3D 全域存檔仍可記住目前正在看的既有板件（`active_part`），方便下次開啟回到相同板件；這只是導航位置，不會連帶提交未確定的機械修改。
- 如果 3D 草稿新增了尚未確定的新板件，單純存檔不會因為目前選到它就把它偷渡進正式專案。
- 「還原初始值」仍是 AE Factory Defaults，和 ProjectSession 的 loaded baseline / committed / draft 是不同概念。

## 設定值：目前值、儲存為預設值與還原初始值

```text
AE Factory Defaults
        │
        └─「還原初始值」

config.ini（下次啟動預設）
        │ 啟動時讀取
        ▼
SettingsService（目前已確認 Runtime）
        │
        ├─ 主 GUI / 2D / AE compatibility mirror
        │
        └─ 開啟 3D → Settings Draft
                         ├─ 取消 → 丟棄，不改目前 Runtime
                         ├─ 確定 → 提交成新的 Runtime
                         └─ 儲存為預設值 → 只寫 config.ini，當下 Runtime 不變
```

- 主畫面直接修改設定時，該值就是目前已確認 Runtime；2D／3D 後續開啟與 AE 相容層使用同一份 committed settings。
- 3D 內尚未按「確定」的設定仍是草稿。按「取消」或關閉 3D，不會修改目前 Runtime。
- 3D 的「儲存為預設值」意思是**保存給下次啟動使用**；它不等於「確定」目前 3D 草稿。因此按下後再取消 3D，目前畫面與 AE Runtime 仍保持進 3D 前的值。
- 「還原初始值」讀的是程式內建 Factory Defaults，不是最近一次存入 `config.ini` 的使用者預設，也不是 ProjectSession 的 loaded baseline。
- ProjectSession 的 `loaded_baseline / committed / draft` 管專案交易；`SettingsService` 管目前已確認 Runtime Settings。兩者責任不同，不能互相代替。


## 專案 Controller 與 GUI 責任分工

```text
主 GUI（畫面）
├─ 把目前欄位組成 canonical snapshot
├─ 把載入 snapshot 套回欄位
└─ 顯示開啟／儲存檔案對話框
        │
        ▼
Phase6ProjectController
├─ ProjectSession 交易順序
├─ .p6fold payload / read / write
├─ 3D draft Save 保護
└─ project path
```

- 這項改造**不改操作方式**；「開啟專案／儲存專案／另存新檔」與 3D「確定／取消」行為維持原本規則。
- 3D 尚未確定時存檔，Controller 只取正式 committed，不會重新讀主畫面或 3D staged 值。
- 3D 存檔可以記住目前正在看的既有板件，但 `active_part` 只是導航資訊，不能藉此提交未確認板件或尺寸。
- 專案寫入失敗時，目前 project path 保持原值，不會出現「其實沒存成功但後續 Save 已指到新路徑」的狀態。

## 2026-08-23 Workspace Controller 狀態一致性

本版不改變操作方式；主要是讓主 GUI 與 3D/project workspace 使用同一份工作區狀態。

- 板件「是否存在」與「是否勾選 DXF 輸出」仍是兩件不同的事。
- 箱身永遠存在；封頭、封尾、門、底板、指示燈盒／小門的存在狀態由同一 Workspace Controller 管理。
- 3D 刪除某板件後，該板件的 Fold Profile 可保留作重新新增恢復，但不存在期間不會因此重新出現在 2D、3D 或輸出。
- 目前工作板件若被刪除，系統會自動回到仍存在的合法板件。
- 載入另一個 `.p6fold` 專案時，若新專案沒有箱身 Fold Chain，會清除上一個專案的舊 Fold Chain，不會跨專案殘留。
- 本版不更動 ProjectSession 的「確定／取消」交易規則，也不更動 Settings、CornerType、EndCap 幾何公式。

## 2026-08-23 Fold Designer Bridge ownership 收斂

本版不改變任何操作畫面；主要是把 Fold Designer 背後的機械規則從 UI Bridge 移到正式 owner module。

- `phase6_endcap_semantics.py`：負責 Head/Tail 的組合方式、上方 CornerType → assembly type、FW 跟隨／解除跟隨／獨立覆寫，以及 CornerType raw state 轉換。
- `phase6_fold_profiles.py`：負責箱身／封頭／封尾 Fold Profile、linked mating chain、outside-dimension compensation、profile 讀回／merge 與 `FoldProfileSegment` 轉換。
- `fold_designer_bridge.py`：只負責 Fold Designer/Tk 接線、3D transaction、View/Scene adapter；舊程式若仍從 Bridge 取得 profile helper，僅透過同一函式物件的 compatibility re-export，不存在第二套計算。
- `gui.py` 已直接依賴真正 owner；不再透過 Bridge 取得 assembly/FW/Fold Profile 機械規則。
- 本次不變更 `OVERLAY`、linked Fold Chain、CornerType、EndCap FW、DXF 或 `.p6fold` 行為。

## FinalScene 3D 顯示所有權（2026-08-23）

Phase6 3D 現在由 `phase6_final_scene_view.py` 顯示 `PartRenderData`：

- 2D／DXF／3D 共用 manufacturing 已完成的 `material / scene / fold_guides`。
- 3D View 只做 triangulation、Fold Profile 投影、BEND／MARKING／盲孔線條、尺寸標示與視角縮放。
- 3D View 不會重新建立 CUTTING 材料面，也不會自行解析 CornerType。
- 若 Final Part Geometry 顯示失敗，畫面顯示錯誤，不退回舊 renderer 生成另一套幾何。

## 診斷快照資料所有權（2026-08-23）

本版不改變專案讀寫或 3D 操作方式；主要把診斷 JSON 的資料聚合從 Fold Designer Bridge 移到 `phase6_diagnostics.py`。

- 折彎診斷 JSON 維持 `phase6-fold-diagnostic-v1`，欄位格式不變。
- 診斷資料包含目前 draft 設定、Corner 狀態、workspace、active-part query payload，以及 manufacturing 已完成的 FinalScene／material。
- `.p6fold` 內的 `final_geometry` 仍保存每個 existing part 的 payload、scene、material、fold guides 與個別 error。
- 如果某一板件的 Final Geometry 診斷失敗，只會在該板件 row 記錄錯誤；其他板件仍可保存診斷資訊。
- `phase6_diagnostics.py` 不讀 Tk、ProjectSession、SettingsService，也不建立製造幾何；它只序列化 manufacturing 已提供的結果。
- `.p6fold` 的正式 snapshot 與 committed/draft 交易仍由 Project Controller／ProjectSession 管理；Diagnostic Snapshot 只是觀測資料，不會成為另一份 Source of Truth。

## Phase6 設定面板架構（2026-08-23）
3D 設定畫面的通用欄位、進階設定、基準檔資料展開與左側全域控制現在由 `Phase6SettingsPanel` 顯示。這只是 UI 層調整：3D 內修改仍屬 draft，按「確定」才提交主畫面；按「取消」不會提交。CornerType、組合方式與封頭／封尾 FW 仍沿用原本的機械規則與交易流程。

## 3D 板件草稿工作區

3D Fold Designer 內的板件切換、新增、刪除與重新加入，現在由同一個草稿工作區管理。操作方式沒有改變，但內部會確保：

- 切換板件前先保存目前板件的 Fold Profile／孔位與相關草稿，再切換到目標板件。
- 刪除非箱身板件只代表「目前不存在」；其既有 Fold Profile／feature stash 會保留，之後重新加入可恢復原資料。
- 箱身 `box_body` 永遠存在，不能從 3D 工作區刪除。
- 回到板件首頁只清除目前 active／selected，不會把各板件已保存的草稿 stash 清掉。
- 3D 工作區仍屬未確認 transaction；只有按「確定」才會提交到主 GUI，按「取消」或關閉仍會丟棄整次 3D 草稿。
- `.p6fold`、診斷快照與 FinalScene 查詢會使用同一份工作區 presence／active／profile／feature 狀態，避免不同輸出各自拼出不同答案。

## 統一開孔編輯器交易狀態（2026-08-23）

本版不改變統一開孔編輯器的操作方式；主要把原本藏在單一 Tk 方法裡的 Undo、取消、切換頁面與草稿狀態收斂到 `Phase6HoleEditorSession`。

- 新插入孔、拖曳、旋轉、十字基準與距離定位都屬「目前未確認編輯」；按局部「取消」會回到該次編輯前狀態。
- 局部「確定」後才形成 Undo 步驟；「↶ 回上一步」最多保留 50 步，規則與原本相同。
- 刪除孔與「切穿 ⇄ 盲孔」屬立即完成操作，可用 Undo 還原。
- 門板／指示燈盒／小門切換時，上一個頁面未確認的暫時編輯會先取消，避免 transient edit 跟著切到另一個實體板件。
- 「取消全部」會把本次開孔編輯器內已使用過的所有 context 恢復成開啟編輯器時的內容；「確定全部」則保留目前所有 context 結果。
- 圓孔排列仍使用原本的排列幾何公式，但 preview／取消／確定改走同一 Session transaction，避免另外維護第二份 snapshot。
- 本次沒有改變孔型 catalog、CUTTING／BLIND_HOLE 定義、孔位幾何、DXF／NC 或 `.p6fold` schema。


## 統一開孔編輯器：Canvas 顯示與交易狀態分離

本版不改變開孔編輯器的操作方式。使用者仍可直接在 Canvas 上點選孔、拖曳、旋轉、修改十字基準、刪除、Undo、確認或取消。

內部責任現在分成：

```text
Phase6HoleEditorSession
  └─ 孔位草稿／Undo／Confirm／Cancel

Phase6HoleEditorCanvasView
  └─ CanvasTransform／resolved cache／hit-test／參考框顯示

gui.py
  └─ Tk 事件組裝／幾何命令／Door & Indicator 額外預覽
```

- Canvas redraw 與滑鼠 hit-test 共用同一份 transform 與 resolved feature cache，避免畫面看到的位置和點擊命中位置各算一套。
- Door 指示燈盒、小門與 enclosure reference 仍使用原本 manufacturing/domain 規則；只是透過額外繪製 callback 顯示，不會把製造公式搬進 View。
- 「取消全部」仍由 HoleEditorSession 恢復原始孔位；Canvas View 不保存第二份孔位資料。


## 箱身結構型態（2026-08-23）

箱身結構設定位於 **3D Fold Designer → 箱身頁面**，不在全域設定，也不會另外開新頁。

- 預設顯示目前結構型態並鎖定；鎖定時不顯示可調細項。
- 按「解鎖結構」後，可在下列型態間切換：
  - 一體成型
  - 二件式（W 二分）
  - 三件式（W 三分）
  - 三件式（側背分離）
- 各型態自己的設定會保留，來回切換不會清除，可即時比較。

### 二件式（W 二分）

- 只拆原一體式中央 W；左右既有 D／FW／Z 保留。
- W 左、W 右都可輸入，另一邊自動補足；單邊不得小於 50 mm。
- 初次沒有歷史值時預設對半；系統自算可保留 0.5 mm，人工輸入只接受整數。
- 中央接合折邊預設 12 mm、最小 12 mm；50 mm 以上顯示警告但仍允許操作。
- 「截角／避讓」預設收合，需要時再展開調整封頭尾與底板十字截角參數。

### 三件式（W 三分）

- 預設為 `W左=50 / W中=W-100 / W右=50`。
- 左右連動；改 W 中時，左右自動平均補足。
- 兩條接縫沿用 W 二分的接合折邊與十字截角規則。

### 三件式（側背分離）

- 板件為左側板、後面板、右側板。
- 側板成型深度 D 以唯讀資訊顯示。
- 側板後折預設 15 mm，可調。
- 後面板寬補償預設 0.5T，可調；畫面即時計算後面板成型寬。

### 輸出與開孔

- 多件式 2D／3D／DXF 由同一 resolved geometry 產生。
- 每個實體板件各自輸出 DXF。
- 原有開孔不因分件自動移位；切線經過 feature 時會依各板件實際材料邊界裁切。CUTTING 保持 CUTTING，BLIND_HOLE／MARKING／DATUM 保持各自 layer，不會因分件產生假的 CUTTING 封邊。
- 已設定 W 二分／W 三分後若再改全域 W，系統會依最後輸入 driver 重新補全分件尺寸；若新 W 無法滿足合法幾何，該次 W 會被拒絕並恢復上一值。
- 本功能不新增焊接 MARKING，也不建模焊縫間隙。

## FW 連動操作（2026-08-23）

封頭／封尾 FW 現在由操作順序自動決定控制權，不需要先取消「跟隨箱身」才能修改：

1. 初始由**箱身 FW**控制封頭與封尾。
2. 先修改**封頭 FW**：封尾立即跟成相同值；之後繼續改封頭，封尾仍跟隨。
3. 先修改**封尾 FW**則完全相反：封頭跟隨封尾。
4. 當其中一端正在主導時，再手動修改另一端，封頭／封尾就解除連動，之後各自保留自己的 FW。
5. 不論目前由哪一端主導或已經各自獨立，只要重新在**箱身 FW**完成一次選擇／提交，箱身就重新接管，封頭與封尾一起回到箱身 FW。即使重新提交的是相同數值，也算重新接管。
6. 一般切頁、重繪、3D 更新或其他參數變更不會擅自改變 FW 控制權。

箱身對稱折彎的 FW/D/Z 連動改用欄位語意配對，不再依列位置判斷，因此曾經單側增刪折彎後也不會把 FW 值誤寫到其他欄位。
