name: Issue 289 Residual22 Shards

on:
  workflow_dispatch:

permissions:
  contents: read

env:
  PYTHONUTF8: "1"
  CANDIDATE_SHA: "3ee91f61d313f0ff3931a80efebb9c798d1e9328"

jobs:
  parked:
    runs-on: ubuntu-latest
    steps:
      - name: Diagnostic is parked after authoritative full residual GREEN
        run: |
          echo "Candidate=${CANDIDATE_SHA}"
          echo "Full residual authority: run 35044084079 = 592 passed / 22 deselected / 0 failed"
