name: Issue 208 T4 Gate

on:
  pull_request:
    branches:
      - refactor/issue207-pure-drawing-move-20260914
    paths:
      - '.scratch/issue208-t4-checkpoint.md'
  workflow_dispatch:

jobs:
  parser-probe:
    runs-on: ubuntu-latest
    steps:
      - name: Probe
        run: echo T4_WORKFLOW_PARSER_GREEN
