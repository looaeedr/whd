name: Issue226 T0 Knowledge Preflight

on:
  push:
    branches:
      - work/issue225-knowledge-documentation-consolidation-20260914

jobs:
  preflight:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Discover required skills and references
        shell: bash
        run: |
          python tools/phase6_skill_preflight.py \
            --task "T0 Inventory Freeze bootstrap governance guard AI Library documentation metadata authority classification" || true
