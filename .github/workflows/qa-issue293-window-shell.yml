name: QA issue293 window shell GREEN

on:
  push:
    branches:
      - refactor/issue293-window-shell-20260918
  workflow_dispatch:

permissions:
  contents: write

jobs:
  focused:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply window shell extraction
        run: python tools/qa_issue293_window_shell_green.py
      - name: Install pytest
        run: python -m pip install -q pytest
      - name: Verify focused GREEN
        run: python -m py_compile gui.py gui_modules/editors/hole_editor_view.py && python -m pytest -q tests/test_issue293_t5_window_shell_slice.py tests/test_issue293_t5_canvas_authority_slice.py tests/test_issue293_t5_session_authority_slice.py
      - name: Commit GREEN after verification
        shell: bash
        run: |
          git config user.name "looaeedr"
          git config user.email "76246585+looaeedr@users.noreply.github.com"
          git add gui.py gui_modules/editors/hole_editor_view.py
          git diff --cached --check
          git commit -m "refactor(#293): extract editor window shell"
          git push origin HEAD:${GITHUB_REF_NAME}
