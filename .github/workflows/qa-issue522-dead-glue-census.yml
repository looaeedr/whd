name: Issue 522 Dead Glue Census

on:
  push:
    branches:
      - work/issue520-phase6-bridge-residual-reduction-20260923
    paths:
      - .github/workflows/qa-issue522-dead-glue-census.yml

permissions:
  contents: read

jobs:
  census:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Exact tracked-file caller census
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          from __future__ import annotations
          import json
          import re
          import subprocess
          from pathlib import Path

          census_path = Path("docs/superpowers/checkpoints/issue521-a0-residual-ownership-census.json")
          a0 = json.loads(census_path.read_text(encoding="utf-8"))
          candidates = [
              row["name"]
              for row in a0["top_level_symbols"]
              if row["classification"] == "BOUNDARY_RECONSIDERATION_CANDIDATE"
          ]
          if len(candidates) != 327 or len(set(candidates)) != 327:
              raise SystemExit(f"candidate inventory mismatch: {len(candidates)}")

          excluded_paths = {
              ".github/workflows/qa-issue522-dead-glue-census.yml",
          }
          raw = subprocess.check_output(["git", "ls-files", "-z"])
          paths = [Path(p.decode("utf-8", "surrogateescape")) for p in raw.split(b"\0") if p]

          def category(path: Path) -> str:
              p = path.as_posix()
              if p.startswith("tests/"):
                  return "tests"
              if (
                  p.startswith("docs/")
                  or p.startswith("logs/")
                  or p.startswith(".dispatch/")
                  or p.endswith(".md")
                  or p.endswith(".rst")
                  or p.endswith(".txt")
              ):
                  return "docs_evidence"
              if p.endswith(".py"):
                  return "production_py"
              if p.endswith((".json", ".toml", ".ini", ".yml", ".yaml")):
                  return "runtime_text"
              return "other_tracked_text"

          decoded = {}
          for path in paths:
              p = path.as_posix()
              if p in excluded_paths:
                  continue
              try:
                  data = path.read_bytes()
              except OSError:
                  continue
              if b"\x00" in data[:4096]:
                  continue
              try:
                  decoded[p] = data.decode("utf-8", "surrogateescape").splitlines()
              except UnicodeDecodeError:
                  continue

          rows = {}
          for name in candidates:
              pat = re.compile(rf"\b{re.escape(name)}\b")
              definition_pat = re.compile(rf"^\s*(?:async\s+)?(?:def|class)\s+{re.escape(name)}\b")
              refs = []
              definitions = []
              for p, file_lines in decoded.items():
                  path = Path(p)
                  for lineno, line in enumerate(file_lines, 1):
                      if not pat.search(line):
                          continue
                      if p == "fold_designer_bridge.py" and definition_pat.search(line):
                          definitions.append({"path": p, "line": lineno})
                          continue
                      refs.append({
                          "path": p,
                          "line": lineno,
                          "category": category(path),
                          "snippet": line.strip()[:240],
                      })
              runtime_refs = [
                  r for r in refs
                  if r["category"] in {"production_py", "tests", "runtime_text", "other_tracked_text"}
              ]
              rows[name] = {
                  "definitions": definitions,
                  "references": refs,
                  "runtime_references": runtime_refs,
                  "runtime_reference_count": len(runtime_refs),
                  "docs_evidence_reference_count": sum(r["category"] == "docs_evidence" for r in refs),
                  "dead_candidate": len(definitions) == 1 and len(runtime_refs) == 0,
              }

          dead = sorted(name for name, row in rows.items() if row["dead_candidate"])
          live = sorted(name for name, row in rows.items() if row["runtime_reference_count"] > 0)
          malformed = sorted(name for name, row in rows.items() if len(row["definitions"]) != 1)
          payload = {
              "schema": "WHD_ISSUE522_DEAD_GLUE_CENSUS_V2",
              "head_sha": subprocess.check_output(["git", "rev-parse", "HEAD"], text=True).strip(),
              "candidate_count": len(candidates),
              "dead_candidate_count": len(dead),
              "dead_candidates": dead,
              "live_candidate_count": len(live),
              "malformed_definition_count": len(malformed),
              "malformed_definitions": malformed,
              "rows": rows,
          }
          Path("issue522-dead-glue-census.json").write_text(
              json.dumps(payload, ensure_ascii=False, indent=2) + "\n",
              encoding="utf-8",
          )
          md = [
              "# Issue 522 exact-branch exhaustive dead-glue census",
              "",
              f"- HEAD: \`{payload['head_sha']}\`",
              f"- candidates: {len(candidates)}",
              f"- dead candidates: {len(dead)}",
              f"- live candidates: {len(live)}",
              f"- malformed definitions: {len(malformed)}",
              "",
              "## Dead candidates",
              "",
              *([f"- \`{x}\`" for x in dead] or ["- none"]),
              "",
              "## Runtime-reference counts",
              "",
              "| symbol | runtime refs | docs/evidence refs |",
              "|---|---:|---:|",
          ]
          for name in candidates:
              row = rows[name]
              md.append(
                  f"| \`{name}\` | {row['runtime_reference_count']} | "
                  f"{row['docs_evidence_reference_count']} |"
              )
          Path("issue522-dead-glue-census.md").write_text("\n".join(md) + "\n", encoding="utf-8")

          print(f"CANDIDATE_COUNT={len(candidates)}")
          print(f"DEAD_CANDIDATE_COUNT={len(dead)}")
          print(f"LIVE_CANDIDATE_COUNT={len(live)}")
          print(f"MALFORMED_DEFINITION_COUNT={len(malformed)}")
          print("DEAD_CANDIDATES=" + ",".join(dead))
          print("ISSUE522_DEAD_GLUE_CENSUS=GREEN")
          PY

      - name: Upload census artifact
        uses: actions/upload-artifact@v4
        with:
          name: issue522-dead-glue-census
          path: |
            issue522-dead-glue-census.json
            issue522-dead-glue-census.md
          if-no-files-found: error
          retention-days: 14
