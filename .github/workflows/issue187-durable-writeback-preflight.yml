name: issue187-durable-writeback-preflight
on:
  push:
    branches:
      - feat/issue187-3d-output-migration-20260913
    paths:
      - .github/workflows/issue187-durable-writeback-preflight.yml
permissions:
  contents: read
jobs:
  preflight:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify durable-writeback changed-file preflight
        shell: bash
        run: |
          python tools/phase6_skill_preflight.py \
            --task "#187 T2 durable writeback after 3D DXF/STOCK migration; update existing UI Skill, DXF validation Skill, and legacy 2D retirement pitfall; physical presence vs 3D visibility vs DXF export intention; project load; differential baseline QA; 派工" \
            --changed-file '.agents/skills/engineering/UI設計與去AI味/SKILL.md' \
            --changed-file '.agents/skills/engineering/驗證板件與DXF/SKILL.md' \
            --changed-file '個人AI檔案庫/踩坑庫/legacy_2d_entry_retirement_20260910.md' \
            --evidence .scratch/issue187/changed_file_preflight_evidence.md
