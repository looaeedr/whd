name: QA issue293 canvas authority GREEN

on:
  push:
    branches:
      - refactor/issue293-canvas-authority-20260918
  workflow_dispatch:

permissions:
  contents: write

jobs:
  focused:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply canvas authority seam
        run: python tools/qa_issue293_canvas_authority_green.py
      - name: Install pytest
        run: python -m pip install -q pytest
      - name: Verify focused GREEN
        run: python -m py_compile gui.py gui_modules/editors/hole_editor_view.py && python -m pytest -q tests/test_issue293_t5_canvas_authority_slice.py tests/test_issue293_t5_session_authority_slice.py tests/test_issue293_gui_phase2_t5_editors.py::test_issue293_editor_package_reuses_existing_session_and_canvas_authorities
      - name: Commit GREEN after verification
        shell: bash
        run: |
          git config user.name "looaeedr"
          git config user.email "76246585+looaeedr@users.noreply.github.com"
          git add gui.py gui_modules/editors/hole_editor_view.py
          git diff --cached --check
          git commit -m "refactor(#293): route editor canvas authority"
          git push origin HEAD:${GITHUB_REF_NAME}
