name: T1 Box Body canonical lifecycle QA

on:
  push:
    branches:
      - work/box-body-single-source-t1-20260907
    paths:
      - "gui.py"
      - "ae_engine/manufacturing_api.py"
      - "tests/test_box_body_single_source_t1.py"
      - ".github/workflows/t1-box-body-single-source-qa.yml"

permissions:
  contents: read

jobs:
  t1:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: python -m pip install pytest matplotlib -r requirements.txt
      - name: Focused T1 regression
        shell: bash
        run: |
          set +e
          before="$(sha256sum config.ini | awk '{print $1}')"
          xvfb-run -a python -m pytest -q tests/test_box_body_single_source_t1.py > t1.log 2>&1
          rc=$?
          after="$(sha256sum config.ini | awk '{print $1}')"
          cat t1.log
          printf '{"pytest_rc":%s,"config_before":"%s","config_after":"%s"}\n' "$rc" "$before" "$after" > t1_state.json
          if [ "$before" != "$after" ]; then
            echo "config.ini mutated" >&2
            exit 97
          fi
          exit "$rc"
      - name: Upload evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: t1-box-body-single-source-${{ github.run_id }}
          path: |
            t1.log
            t1_state.json
