name: Issue 212 Reconcile Safe Repair V2

on:
  push:
    branches:
      - repair/issue212-reconcile-safe-v2-20260915

permissions:
  contents: write

jobs:
  repair:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Rebuild exact safe candidate
        run: python .scratch/issue212_reconcile_repair.py
