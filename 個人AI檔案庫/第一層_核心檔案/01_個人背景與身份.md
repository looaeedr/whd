# 👤 01. 個人背景與身份 (Profile & Background)

## 📌 基本資訊
- **使用者稱呼**：**ㄚ凱**
- **主要角色**：**資深工程師 / CAD 與鈑金自動化系統架構師**
- **所在時區**：UTC+8 (台灣/台北時間)
- **工作語言**：繁體中文 (Traditional Chinese - 台灣習慣技術用語)

---

## 🚀 核心專案資產：CAD 拆圖與 WHD 展開一體化系統
- **最新專案路徑**：`Z:\Ollama-整合whd\`
- **系統架構**：
  - **拆圖大腦 (`main.py` + `modules/`)**：大圖框自動感應 (`A$C784A6902`)、洋紅打叉廢圖剔除、多盤體 X 軸切分、雙軸氣切視圖分離、跨框鄰框自動抓取補全、底座獨立拆分 (`detect_child_bottom_base()`)。
  - **展開引擎 (`ae_engine/` 套件)**：以 `manufacturing_api.py` 為統一對外接口，內嵌 Shapely 幾何拓撲布林展開 (`sheetmetal_geometry.py`)、圖元場景 (`sheetmetal_drawing.py`)、開孔特徵 (`sheetmetal_features.py` + `hole_catalog.py`) 與 `contracts.py`。
  - **輸出端**：獨立視圖 DXF、JPG 快照、CSV 尺寸報表（加工圖路徑跟隨輸出根目錄）。
- **輔助研發庫**：`Z:\whd-corner-new-engine-implemented\`（WHD 獨立演算法研發與單元測試基準）。

---

## 🛠️ 技術棧與工具鏈
- **CAD / CAM 與幾何演算**：
  - AutoCAD / DWG / DXF 工具鏈、ezdxf (AC1032)
  - Shapely (幾何多邊形布林運算、拓撲分析)
  - 雷射切割 CAM 軟體（CypCut 柏楚、Trumpf、Bysoft 等）
- **程式語言與開發框架**：
  - Python 3.8+ (Matplotlib, Pillow, Pandas, OpenCV, Tkinter, Pytest)
  - Windows PowerShell / CLI 自動化環境
- **AI 應用與工程規範**：
  - 跨模型協作 (Claude 3.5 Sonnet / GPT-4o / Gemini)
  - 嚴格 TDD 與 Pre-Edit 備份保護機制

---

## 💡 工作哲學與原則
1. **Geometry 是唯一真相來源 (Single Source of Truth)**：GUI 預覽與 DXF 導出必須源於同一幾何核心，確保畫面所見即為 DXF 所出。
2. **拓撲與製造規範分離**：Topology 定義物理折彎；Factory Policy 定義裝配退讓與工藝扣量。
3. **可靠高於速度**：備份優先（`BACKUP/`）、嚴謹驗證無誤後才交付。
