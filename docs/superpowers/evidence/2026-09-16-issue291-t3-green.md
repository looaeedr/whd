---
whd_doc_role: REFERENCE
whd_contract: issue291-t3-green-evidence
whd_canonical: null
whd_schema: WHD_DOC_META_V1
---

# Issue #291 T3 Selector Extraction — GREEN Evidence

Date: 2026-09-16

## Accepted predecessor

- #290: `f11ab72850d893f5935ad869ab8ae9205fc0cd92`

## RED proof

- Structural RED run: `35058414222`
- Result: `5 FAIL / 0 PASS`
- Root cause was the expected pre-extraction structure: missing `gui_modules/parts`, inline physical subtab construction, root methods not delegated, and `gui.py` above the reconciled T3 LOC gate.

## GREEN implementation

- Implementation commit: `05adca74e233ec795937a1e671d7c872b044782a`
- Message: `refactor(#291): extract selector and box body subtabs`
- Focused package created under `gui_modules/parts`.
- `gui.py` retains compatibility method names only as thin delegation seams.
- Physical box-body Notebook construction delegates to `build_box_body_piece_selector`.
- `_phase6_set_part_presence` remains in the host/controller seam.
- No geometry / placement authority was intentionally moved.
- Stable physical-part IDs remain `box_body:left_side`, `box_body:back`, `box_body:right_side`.

## In-run structural verification

The isolated apply run `35083105567` used the same canonical Python runtime pattern as the official #291 structural workflow (`actions/setup-python@v5`, Python 3.12). Before committing the implementation it successfully completed:

- extraction body
- `python -m py_compile gui.py gui_modules/parts/selector.py gui_modules/parts/subtabs.py`
- `python -m pytest -q tests/test_issue291_gui_phase2_t3_selector.py`
- commit / push
- removal of all temporary apply runners (`issue291-t3-apply*.yml`)

This document intentionally creates a normal user-authored commit after the bot implementation commit so the official branch workflows receive a fresh push event and produce concrete post-implementation RUN identities.

## Remaining acceptance evidence

Still required before closing #291:

- official post-implementation structural run GREEN
- reconciliation run GREEN
- selector/subtab characterization
- receiving ↔ vault transition parity
- multipart child-selection parity
- hidden/visible selector state parity
- relevant Xvfb interaction regressions
- Headless state-selection regressions
- protected invariant / drift checks
- temporary QA/helper cleanup confirmation
