# Issue #126 R1 durable checkpoint

## Identity
- issue: #126 T4 Drawing Area / Visual Hierarchy / workspace layout
- branch: `work/issue-126-workspace-layout-20260912-r1`
- production baseline: `f85e0ba0047bcf3d9d447ecf7d365484267c29d0`
- latest tested head before this checkpoint: `3d219e743f96b35bb3672fa5c3c094c04041ff71`

## Authority boundary
This task is presentation/layout-only. It must not create a second authoritative workspace state owner, must not change geometry/domain/projection/unfold/placement/manufacturing/DXF authority, and must not mutate protected baselines or `config.ini`.

## Implementation
- `fold_designer_bridge.py::_phase6_build_persistent_top_area()` bounds the left inspector/sidebar width and prevents horizontal geometry propagation from allowing it to consume the central workspace.
- the central `self.right` workspace remains the expanding presentation area.
- no geometry, projection, redraw authority, save/load authority, manufacturing rule, DXF rule, or baseline file was changed.

## Test progression
- genuine RED on latest production established that the current left sidebar reached ~441 px and violated the bounded-sidebar workspace contract.
- focused committed-head contract: `tests/test_issue126_workspace_layout.py` -> **3 PASS / 0 FAIL**.
- five unrelated corner-parameter UI failures were found by the broad regression wave.
- untouched production baseline `f85e0ba0...` reproduced those exact five failures -> **5 FAIL**, proving they pre-existed #126 and are not #126 regressions.
- final acceptance run: `34685454277` @ `3d219e743f96b35bb3672fa5c3c094c04041ff71` -> **SUCCESS**.
- final focused workspace contract: **3 PASS / 0 FAIL**.
- existing UI/view regression wave after deselecting only the five exact baseline-proven failures: **201 PASS / 5 DESELECTED / 0 FAIL**.
- Phase6 changed-file preflight: GREEN.
- production ancestry / merge-base lock: GREEN.
- protected invariant postcheck: `config.ini` and all files under `基準檔` unchanged; clean worktree.

## Baseline-proven unrelated failures
The only deselected tests are the exact five tests reproduced on untouched production:
1. `tests/test_corner_parameter_lock.py::test_fold_designer_corner_parameters_default_locked_and_unlock_does_not_mutate_state`
2. `tests/test_corner_parameter_lock.py::test_3d_corner_parameters_default_locked_for_known_model_and_unlock_without_mutation`
3. `tests/test_corner_parameter_lock.py::test_fold_designer_locked_parameters_have_no_hidden_editor_and_unlocked_can_adjust_amount`
4. `tests/test_corner_parameter_lock.py::test_3d_known_model_locked_guard_and_unlocked_parameter_edit_preserves_type`
5. `tests/test_corner_type_phase6_integration.py::test_corner_panel_is_visible_for_known_and_custom_models_with_type_locking`

These failures remain separate existing debt and must not be fixed by changing #126 production layout code.

## Resume / next action
State is **RUNNING**, not COMPLETE. This checkpoint is not a stop signal.
Next actions are mandatory and autonomous:
1. remove temporary `.github/workflows/issue126-owner-preflight.yml`;
2. compare tested head -> cleaned head and baseline -> cleaned head;
3. require final durable diff to contain only the intended #126 production/test/checkpoint files;
4. re-read issue #126 integration authority;
5. create PR to `cleanup/2d-3d-sync`, merge if no human integration gate exists;
6. re-read production HEAD and comment integration evidence on #126;
7. continue to #127 from the new production baseline.
