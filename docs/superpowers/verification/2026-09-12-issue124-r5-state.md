# Issue #124 R5 latest-target replay state

- Production target: `cleanup/2d-3d-sync`
- Exact baseline: `d493b2e0d7404233b6ed2c681caf7d28c73b30f6`
- Replay branch: `work/issue-124-replay-latest-20260912-r5`
- Changed-file preflight: run `34679251453` SUCCESS.
- Genuine latest-target RED: run `34679297227`; headless 4 failed/5 skipped, Xvfb 9 failed, missing Structure Tree projection/refresh and `persist=False` text-scale config anti-echo behavior.
- Replay run: `34679475555` SUCCESS; exact target rechecked, Phase6 gate GREEN, replay-owned concurrency gate GREEN, final accepted R4 files replayed.
- Replay production commit: `a6722b9aa22e17368fdc33c6f17a7371466ad26c`.

## Authority / scope

`designer_workspace.available_parts` and stable physical-part IDs remain authoritative. The Structure Tree is projection/navigation only. Existing assembly visibility vars remain visibility owners. No geometry, topology, manufacturing formula, relief registry, DXF authority, or Save/Reload schema is derived from validation or moved into the tree.

## Durable pitfall

External UI text-scale application with `persist=False` must be guarded against Designer trace echo so runtime synchronization cannot write `config.ini`. The related durable Skill marker and AI pitfall file are replayed with the production fix.

## Final acceptance

- Final run `34679520208`: terminal SUCCESS.
- Exact production-target / intended-diff gate: GREEN.
- Actual-branch Phase6 changed-file preflight: GREEN.
- Headless Structure Tree contract: **4 passed / 5 skipped / 0 failed**.
- Finished-part DXF / resolved manufacturing / persistence matrix: **53 passed / 10 skipped / 0 failed**.
- Xvfb Structure Tree contract: **9 passed / 0 failed**.
- Runtime visual identity evidence: `BOX_BODY_TREE_CHILDREN = ('part:box_body:left_side', 'part:box_body:back', 'part:box_body:right_side')`; selected physical identity resolved to `box_body:back`.
- Visual checks passed at 1200x850 and 760x420 with large text scale; Structure Tree remained mapped and the selected child retained a real bbox.
- `config.ini` SHA-256 before/after: `980eab68d4a1732a5313b22329852dfc9691c83e4e2a64cccd18022afae4ee67` unchanged.
- All **24** files under `基準檔` remained byte-identical; `INVARIANTS=PASS`.
- Visual evidence artifact: `issue124-r5-final-visual-evidence`, artifact ID `10293037122`, archive SHA-256 `6e08e220a3e1d232a96a61fe07f1b6e1366be200a34ef07efa10643ad048181e`.
- Final acceptance did not change geometry, topology, manufacturing formula, relief registry, DXF authority, or Save/Reload schema.

R5 is acceptance-GREEN and may proceed only through cleanup + tested-to-cleaned drift audit + integration gate.
