## 16:03 統一開孔編輯器 Canvas View ownership

- 新增 `phase6_hole_editor_canvas_view.py`，集中 Canvas transform、resolved-feature cache、hit-test、selected crosshair 與浮動 reference overlay。
- `_open_unified_hole_editor()` 移除 `transform_box / hit_index / hide_overlays / place_reference_overlays / resolved_canvas_rect` 本地實作。
- mouse down/drag/right-click 改由 `canvas_view.canvas_to_world()` 與 `canvas_view.hit_test()` 使用同一份 render cache。
- Door enclosure／indicator 顯示仍由 GUI adapter 組 `draw_extra`，新 View 不 import manufacturing owner。
- 真 Tk 新增「點既有孔 → 拖曳 → Cancel All 還原」回歸。
- editor method：1431 行 → 1333 行；巢狀函式 61 → 57。
- 完整可執行 suite：354 passed、2 skipped、4 deselected、0 failure。

### 00:28:00 Phase6 Fold angle / 主2D回寫 / 板件單擊操作修正
- **變更檔案**：`fold_designer_bridge.py`, `gui.py`, `tests/test_phase6_linked_fold_chain_and_parts.py`, `tests/test_phase6_3d_view_regressions.py`, `tests/test_phase6_ui_state_regressions.py`
- **修改摘要**：
  - 封頭/封尾 linked Fold Chain 不再把來源 angle 強制固定成 -90；改為傳遞箱身對應 bend boundary 的實際輸入 angle。
  - 保留既有 UI/engine 角度符號慣例，但保證 UI→engine→UI round-trip 回到原輸入；3D 不再二次換算。
  - 移除先前錯誤加入的 orthographic projection 補丁；視角不介入折彎角度。
  - 修正 Phase6 commit 後主 GUI `update_calculations()` linked branch 未初始化 `baseline` 導致結果全部顯示 `-`；主 2D 現直接量 committed FinalScene。
  - 板件按鈕單擊一次直接進入；無 Edit/雙擊流程；「刪除板件」保留在板件按鈕區正下方。
- **真檔驗證**：`自訂(2).p6fold` commit 後封頭/尾主 2D = `422.00 × 284.00 mm`。

### [01:01:00] 個人AI檔案庫：固化箱身／封頭尾組合理解與本次 AI 誤判
- **變更檔案**：`個人AI檔案庫/第一層_核心檔案/04_全域AI協作規則.md`
- **變更檔案**：`個人AI檔案庫/README.md`
- **變更檔案**：`個人AI檔案庫/第二層_專案與SOP/04_WHD鈑金展開幾何引擎規範.md`（修復原 ZIP 錯誤的 `#U...` 轉義 entry，恢復 README 宣告的中文路徑）
- **變更檔案**：`個人AI檔案庫/第二層_專案與SOP/06_踩坑記錄與防錯經驗庫.md`
- **備份檔名**：`BACKUP/20260823-010100-04_全域AI協作規則.md`
- **備份檔名**：`BACKUP/20260823-010100-個人AI檔案庫-README.md`
- **備份檔名**：`BACKUP/20260823-010100-04_WHD原始編碼檔.md`
- **備份檔名**：`BACKUP/20260823-010100-06_踩坑記錄與防錯經驗庫.md`
- **修改摘要**：記錄本次 AI 把「未重新驗證」誤判為「已確認機械規則可能錯誤」的實際錯誤與循環論證根因；固化箱身接合外側折連動、上方 CornerType 裝配語意、貼外 1T、高度尺寸鏈與 5 段箱身對應封頭／封尾 profile，並加入機械真值對程式／測試的證據優先級規則。

### [01:27:05] 個人AI檔案庫：箱身三開口實體關係與 AI 誤判固化
- **變更檔案**：`個人AI檔案庫/第二層_專案與SOP/04_WHD鈑金展開幾何引擎規範.md`
- **變更檔案**：`個人AI檔案庫/第二層_專案與SOP/06_踩坑記錄與防錯經驗庫.md`
- **變更檔案**：`個人AI檔案庫/第一層_核心檔案/04_全域AI協作規則.md`
- **備份時間戳**：`20260823-012704`
- **修改摘要**：固化箱身 `FW-D-W-D-FW` 折起後形成背面+左右側面、留下正面/上端/下端三個開口；門補正面、封頭補上端、封尾補下端。記錄 AI 曾錯把封頭/封尾理解成箱身前後折或單側 Fold Chain 延伸。非對稱外加折「到底跟誰」目前刻意標成待使用者確認，不先猜。並明確修正 `assembly_type` 只能是 mirror，不得反向覆寫上方 CornerType。

### [01:38:00] AI 檔案庫：非對稱 Fold 必須依折後形態判斷，型態擴充暫緩
- **變更檔案**：`個人AI檔案庫/第二層_專案與SOP/04_WHD鈑金展開幾何引擎規範.md`
- **變更檔案**：`個人AI檔案庫/第二層_專案與SOP/06_踩坑記錄與防錯經驗庫.md`
- **修改摘要**：固化「封頭／封尾是否跟隨某 Fold 的依據是折後實體形態，而不是展開圖左右、折數或 segment index」；非對稱 Fold 會需要新增多個接合型態，依使用者決定目前暫不處理，禁止 AI／程式先猜 mapping。

### [01:43:00] 修正 assembly_type 覆寫上方 CornerType 的資料真值 Bug
- **變更檔案**：`fold_designer_bridge.py`, `tests/test_phase6_shared_assembly_and_dimensions.py`
- **修正內容**：`Head/Tail top CornerType` 改為箱體組合的 authoritative mechanical source of truth；`assembly_type` 僅作 UI／相容性 mirror。只有舊檔完全沒有可用 Top CornerType 時，才以 `assembly_type` 作 fallback。
- **回歸案例**：當檔案同時保存 `assembly_type=INSERT`，但 Head/Tail 四個 Top CornerType 皆為 `OVERLAY` 時，解析結果必須為 `OVERLAY`；載入後 Top CornerType 與各側 `amount_t` 必須保持不變。
- **針對性測試**：`tests/test_phase6_shared_assembly_and_dimensions.py` → `25 passed`。
- **實際載入流程模擬**：衝突資料解析為 `OVERLAY`；Head/Tail 四個 Top CornerType 保持 `OVERLAY`，`amount_t` 分別保持 `1.25 / 1.75 / 1.5 / 1.0`。
- **整體回歸（校正無 DISPLAY 環境並排除 2 個既有測試基礎問題）**：`175 passed, 12 skipped, 2 deselected`。
- **兩個既有測試基礎問題**：
  1. `test_opening_phase6_designer_does_not_execute_legacy_renderer` 的 skip 分支使用 `pytest.skip` 卻未 import pytest；與本次程式修正無關。
  2. `test_uploaded_custom_project_proves_legacy_scene_was_not_using_saved_five_segment_chain` 硬編碼舊 regression fixture 必須有 8 條 stored BEND；目前使用者上傳的 `自訂(3).p6fold` 已是 4 條，不能當成該舊 fixture。
- **範圍限制**：非對稱外加 Fold 的 EndCap mapping 仍依使用者決定暫緩；本次未新增任何形態或配對規則。

### [02:09:00] 自訂 UI / 箱身組合方式 / 對稱折彎 / 截角縮圖方向修正
- **變更檔案**：`fold_designer_bridge.py`, `gui.py`, `tests/test_phase6_ui_state_regressions.py`, `DELIVERY_README.md`, `修改日誌/20260823.md`
- **問題 1：自訂仍顯示基準檔區**：新增 `_phase6_should_show_baseline_data()`；自訂模式完全不建立「基準檔開孔資料」區，已知型號仍保留。
- **問題 2：箱身組合方式無法選**：`_phase6_invalidate_corner_pages()` 不再排除 `box_body`；已知型號切自訂時箱身頁會重建，Combobox 依 `_corner_editable=True` 變為 `readonly` 可選。
- **問題 3：對稱折彎核選消失**：箱身頁補回「對稱折彎」控制，直接接回既有 `v_sy → state.symmetric → apply_mirror()`；沒有新增第二套對稱演算法。
- **顯示調整：2D 截角類型小圖上下顛倒**：新增 `_corner_preview_canvas_point()`，只翻轉縮圖 canvas Y 映射；不修改 CornerType、實際 2D/3D geometry 或 DXF。
- **TDD**：5 個新 regression 先以 `5 failed` 重現，再修改程式後為 `5 passed`。
- **實際 Tk smoke**：金庫型 → 自訂後，組合方式 `readonly`、對稱折彎存在、基準檔開孔資料不存在。
- **完整 Xvfb 回歸**：排除 4 個硬編碼 `/mnt/data/自訂.p6fold` 的舊 fixture 案例後：`188 passed, 2 skipped, 4 deselected`。
- **config.ini**：未修改；前後 SHA256 均為 `5947c847ab96a6d739d464d75f50457300ac2cd240e232eb0f2fe1d53c41683d`。
- **範圍限制**：本輪未處理非對稱額外 Fold 的形態分類／EndCap mapping。

### 03:08 變更紀錄：Phase6 UI 交易與 OVERLAY 無 X 折彎
- **變更檔案**：`gui.py`、`fold_designer_bridge.py`、`tests/test_phase6_ui_state_regressions.py`、`個人AI檔案庫/第一層_核心檔案/04_全域AI協作規則.md`、`個人AI檔案庫/第二層_專案與SOP/04_WHD鈑金展開幾何引擎規範.md`、`個人AI檔案庫/第二層_專案與SOP/06_踩坑記錄與防錯經驗庫.md`、`DELIVERY_README.md`。
- **備份**：修改前已追加保存至 `BACKUP/`；本輪不修改 `config.ini`。
- **修改摘要**：
  - 組合方式選擇事件不再 destroy/rebuild 當前 box_body 頁，只 invalidate Head/Tail dependent pages，修正選完組合類型後控制項／頁面消失。
  - `固定孔 / 封尾固定孔` 移出進階設定，只保留在基準檔專屬區；自訂模式不顯示。
  - 「對稱折彎」移到 Fold/BEND 編輯區；對稱開啟時刪除一側可刪 segment 會連同 mirror segment 同一交易刪除，受保護則整筆取消。
  - 3D Fold Designer 使用 modal `transient + grab_set` 鎖住主 2D，避免 draft/committed state 並行修改。
  - `OVERLAY` EndCap 取消左右 X 向 Fold/BEND：2D/3D/FinalScene 同源，3D Fold editor 隱藏 X 軸頁；切回 INSERT/INSERT_OVERLAY 恢復正常 X topology。
  - 主 GUI 未曾開過 3D 時也會依 assembly type 建立正確 EndCap profile，避免 2D 繞回 legacy X folds。
  - 本輪 03:08 當下先保留現有下方 `CROSS extra_cut 0.5T`；後續 03:29 已確認 1.5T 可直接使用既有 `CROSS + EXTRA_CUT + direction + amount_t` 表達，不需要新增 CornerType。
- **驗證**：Xvfb 全套回歸（排除 4 個缺少 `/mnt/data/自訂.p6fold` 的舊 fixture）`202 passed, 2 skipped, 4 deselected`；`py_compile` 通過；`config.ini` SHA256 前後一致 `5947c847ab96a6d739d464d75f50457300ac2cd240e232eb0f2fe1d53c41683d`。
### 03:29 變更紀錄：截角規格完整固化 / OVERLAY 下方預設 WIDTH+1.5T
- **變更檔案**：`ae_engine/corner_type_ui.py`、`gui.py`、`fold_designer_bridge.py`、`tests/test_phase6_shared_assembly_and_dimensions.py`、`tests/test_phase6_ui_state_regressions.py`、`個人AI檔案庫/第二層_專案與SOP/04_WHD鈑金展開幾何引擎規範.md`、`個人AI檔案庫/第二層_專案與SOP/06_踩坑記錄與防錯經驗庫.md`、`docs/superpowers/README.md`、`docs/superpowers/specs/2026-08-21-corner-type-semantic-assembly-design.md`、`docs/superpowers/verification/2026-08-21-corner-type-semantic-assembly-verification.md`、`DELIVERY_README.md`。
- **截角模型文件化**：正式區分 `CornerType / cross_mode / direction / amount_t` 四層；`CROSS + EXTRA_CUT` 是通用避讓幾何，不因避讓對象不同就新增 CornerType。
- **OVERLAY 下方預設**：使用者明確選貼外時，Head/Tail 下方重設為 `CROSS + EXTRA_CUT + WIDTH + 1.5T`；主 GUI 與 Fold Designer 同步。
- **預設不是硬規則**：普通 load/refresh 不重套；人工改成 `HEIGHT / BOTH / 其他 amount / STANDARD` 都必須保存。只有再次明確選 OVERLAY 才重新套預設。
- **初始化與同步分離**：新 GUI state 初始化時明確要求下方預設；普通 `apply_box_assembly_type()` 不再把合法的 `CROSS+STANDARD` 誤判成未設定。
- **Superpowers 補齊**：本輪不再只更新個人 AI 庫；README、CornerType spec、verification 三份同步更新。
- **TDD**：新增 explicit OVERLAY reset、主 GUI reset、人工 HEIGHT/amount 保存、人工 STANDARD 保存等 regression；先 RED，再 GREEN。
- **驗證**：Focused `80 passed`；完整 Xvfb `207 passed, 2 skipped, 4 deselected`；`py_compile` 通過。
- **config.ini**：未修改，SHA256 維持 `5947c847ab96a6d739d464d75f50457300ac2cd240e232eb0f2fe1d53c41683d`。


### 07:16 使用說明書：新增截角類型操作說明
- **新增檔案**：`使用說明書.md`
- **更新檔案**：`docs/superpowers/README.md`
- **內容**：以樹狀方式整理正式 `CROSS / OVERLAY / INSERT / INSERT_OVERLAY`；詳細列出 `STANDARD / RETAIN / EXTRA_CUT`、`WIDTH / HEIGHT / BOTH`、`amount_t`，並補充封頭／封尾上方組合截角、下方加工截角與舊 `C01~C04` 相容對照。
- **範圍**：純文件更新；未修改任何 Python 程式與 `config.ini`。
### 07:44 變更紀錄：修正 OVERLAY 封頭／封尾仍為 422 的 structural blank Bug
- **根因 1（Bridge）**：貼外雖拿掉左右 X angle，但仍把舊 INSERT 型 `yl1 + (W-4T) + yr1` 合成單一 flat span，`W=400,T=2,yl1=yr1=15` 因而仍為 `422`。
- **根因 2（AE structural）**：`build_unknown_endcap_result()` 的 CUTTING/material 外框仍固定使用 `W-4T+|yl1|+|yr1|`；authoritative Fold Profile 只會替換 BEND，無法修正已錯的材料外框。
- **修正**：OVERLAY Head/Tail `endcap_w_flat` 直接使用 `W`；自訂 EndCap 上方兩角為 OVERLAY 時 structural width 直接使用 `W`、`left_fold/right_fold=0`，並移除 structural `left/right` bends。
- **局部截角邊界**：下方 `CROSS + EXTRA_CUT + WIDTH + 1.5T` 只改角落 CUTTING，不參與整張材料寬度。
- **真檔驗證**：`自訂(4).p6fold` 雖保存舊 workspace `endcap_w_flat=422`，載入後 Head/Tail X profile 都重建為 `400`，FinalScene material bounds 為 `400×284`，主 GUI 顯示 `400.00×284.00 mm`。
- **TDD**：Bridge 與 AE structural regression 都先確認 RED（422 ≠ 400），再完成 GREEN。
- **config.ini**：未修改。
- **完整回歸**：Xvfb `208 passed, 2 skipped, 4 deselected`。
### 08:01 變更紀錄：浮點顯示正規化／下方截角縮圖方向恢復
- **UI 浮點顯示**：修正 `400.0000000000006` 類二進位浮點殘差直接出現在介面；近整數以 `1e-9` 容差只在文字層顯示成整數，`400.25` 等合法小數保留。
- **截角縮圖方向**：上方／左上／右上維持目前垂直翻轉；下方／左下／右下恢復原始方向。四種小型 CornerType 圖與大型 preview 同步切換。
- **幾何邊界**：上述兩項都只屬 UI；未修改 CornerType、FinalScene、CUTTING/BEND、3D/DXF 製造幾何。
- **TDD**：浮點顯示與下方縮圖方向皆先確認 RED，再完成 GREEN；focused UI/截角回歸 `62 passed`。
- **config.ini**：未修改。


### 08:15 變更紀錄：刪除板件 Physical Presence 全鏈同步＋兩項 UI 修正收尾
- **Physical presence 單一真值**：`existing_parts` 改為板件實際存在狀態；DXF checkbox 只控制「存在板件是否輸出」，不再反向決定板件有沒有。
- **刪除後零佔位**：左側「展開尺寸計算結果」對應整排直接 `pack_forget`；輸出零件 checkbox 整排也消失，不留空白。
- **2D/3D 同步**：右側 2D tab 維持 hidden；3D available_parts 已有刪除機制；已刪除板件不再因 stash profile 被建立 FinalScene/RenderData。
- **輸出雙保險**：`export_selected_dxf()` 除 checkbox 外再檢查 physical presence；stale checkbox=True 也不能輸出不存在板件。
- **保存/重載**：Fold Designer snapshot 與 `.p6fold` 繼續保存 exact `existing_parts`；取消 DXF 輸出不再誤刪物理板件。profile stash 可保留供重新新增恢復，但不能作為存在證據。
- **真檔 `自訂(4).p6fold`**：檔內只有 `box_body/head/tail`；修正版載入後 Door/底板/指示燈盒/小門左側結果與輸出列皆零佔位，Door/底板右側 tab hidden，重新建立 snapshot 仍只有三片。
- **同輪收尾 UI**：`400.0000000000006` 近整數殘差顯示成 `400`；下方 CornerType 小圖恢復原始方向，上方維持翻轉。兩項都不改製造幾何。
- **TDD / regression**：新增零佔位、stale checkbox export guard、stashed profile no-scene、output checkbox≠presence 等 regression；完整 Xvfb `216 passed, 2 skipped, 4 deselected`。
- **config.ini**：未修改，SHA256 `5947c847ab96a6d739d464d75f50457300ac2cd240e232eb0f2fe1d53c41683d`。

### 08:48 變更紀錄：補齊 .p6fold GUI 讀檔全鏈
- **根因**：程式已有 `.p6fold` parser、`load_phase6_project()`、argv／Windows 雙擊開檔，但 Fold Designer Footer 只有「存檔」，沒有「讀檔」，操作上形成能存不能讀。
- **修正**：Footer 改為 `[讀檔] [存檔]`；讀檔先驗證 `.p6fold`，再關閉目前 modal 3D draft，透過主 GUI authoritative loader 還原全專案並重建 3D 到保存的 active part。
- **資料鏈**：全域尺寸、CornerType、Fold profile、features、`existing_parts`、2D/3D、輸出選項皆沿用既有 `load_phase6_project()`，未建立第二套 restore 邏輯。
- **真檔驗證**：使用 `自訂(4).p6fold` 經 3D「讀檔」載入後，`W/H/D=400/600/250`、physical parts 僅 `box_body/head/tail`、Door 輸出為 False、3D available parts 同步為三片。
- **config.ini**：未修改。
- **完整回歸**：Xvfb `218 passed, 2 skipped, 4 deselected`。


### 09:05 變更紀錄：全域專案讀寫＋CornerType 參數鎖
- **全域專案操作**：`.p6fold` 的 `開啟專案 / 儲存專案 / 另存新檔` 移到主視窗左上角；3D Footer 移除專案讀／存按鈕。各板件頁／3D 只保留自己的 `套用 / 確定 / 取消` transaction。
- **開啟專案**：GUI 開啟與 Windows 雙擊／argv 共用既有 authoritative loader；GUI 開啟不強迫進 3D。
- **截角參數鎖**：主 2D 與 3D 細參數預設鎖定並隱藏；解鎖只改 UI editable state，不修改 Corner state，讀檔後回鎖。
- **已知盤型**：factory CornerType 類型維持唯讀；同 type 細參數可解鎖、保存／讀回，並送到主 2D、3D 與 baseline manufacturing。
- **基準資料保留**：已知盤型 corner policy 覆寫仍走 baseline stretched path，保留固定孔／MARKING／secondary entities，只覆蓋 structural outline/BEND。
- **固定板件**：指示燈盒／指示燈小門維持真正唯讀，不提供解鎖。
- **驗證**：Focused `102 passed`；完整 Xvfb（排除 4 個硬編碼 `/mnt/data/自訂.p6fold` 的舊 fixture）`231 passed, 2 skipped, 4 deselected`；`py_compile` 通過。


## 10:32 參數鎖實際可見性＋3D 全域專案列修正
- 修正上一版只驗內部 lock flag、實際畫面仍攤開部分細節的缺口。
- 2D/3D 鎖鈕改為明確 `參數鎖定 / 參數解鎖`；鎖定時細部參數與 `左右相同` 等進階控制真正移出版面。
- 3D 為 modal，補上左上角全域 `開啟專案 / 儲存專案 / 另存新檔`；Footer 仍只負責目前板件 transaction。
- 3D Save As 後同步 project path；後續 Save 直接覆存，不再重問路徑。
- 回歸新增 visual/layout contract 與 3D Save As→Save current path 驗證。

### 10:55 掃描深模組 Skill／HTML 模板繁體中文強制化
- **根因**：原版 `.agents/skills/engineering/掃描深模組/SKILL.md` 明確要求報告使用 `plain English`，`HTML-REPORT.md` 也以 `lang="en"`、英文欄位與英文建議徽章作為預設模板；因此即使專案要求繁體中文，執行 Skill 時仍可能被英文模板帶偏。
- **Skill 修正**：加入最高優先的繁體中文語言規則、啟動前來源自檢、輸出前語言閘門；要求執行前完整讀取 `HTML-REPORT.md`，不得依記憶生成。
- **模板修正**：`HTML-REPORT.md` 全面改為繁體中文預設，`<html lang="zh-Hant-TW">`；頁首、候選卡片、修改前／修改後、問題、解法、收益、建議強度、最高優先建議與 Mermaid 範例全部中文化。
- **自動檢查**：新增 `check_zh_tw_report.py`，可用 `--skill-sources` 驗證 Skill／模板來源，也可直接檢查產出的 HTML 報告；偵測舊版英文 UI 標籤與錯誤語系。
- **TDD**：新增來源語言測試；先確認缺少來源驗證函式時 RED，再完成 GREEN。最終 `5 tests OK`，Skill／模板來源檢查通過，既有繁體中文報告修正 `lang` 後交付檢查通過。
- **範圍**：未修改 Phase6 製造幾何、GUI、DXF、NC 或 `config.ini`。


### 11:28 EndCap 最終幾何唯一所有權／基準拉伸 topology 收斂
- **目標**：將封頭／封尾的 CornerType、Fold Profile、legacy scalar folds 在 AE 邊界一次解析；2D／3D／DXF 只消費同一 `PartRenderData`／Final Scene，不再讓 GUI、Bridge、structural builder、exporter 各自重算。
- **新增唯一語意 resolver**：`EndCapAssemblySemantics`、`resolve_endcap_assembly_semantics()`、`resolve_endcap_policy_assembly_semantics()`；`INSERT / OVERLAY / INSERT_OVERLAY` 的外高占用、X topology、箱身側外折語意集中於 `ae_engine/sheetmetal_geometry.py`。`endcap_outer_thickness_factor()` 也改走同一 resolver，左右上方 CornerType 矛盾會直接拒絕。
- **新增唯一 request resolver**：`ResolvedEndCapRequest`、`resolve_endcap_request()` 集中 Fold Profile precedence、legacy scalar fallback、CornerType 與 profile 衝突處理。
  - `OVERLAY` 強制 flat X、左右有效 fold=0，殘留 folded X profile 直接丟棄。
  - `INSERT / INSERT_OVERLAY` 強制 folded X；若舊檔殘留 `endcap_w_flat`，丟棄該 profile 並回到 canonical scalar folds。
  - 沒有 CornerType policy 時，Fold Profile 才可自行決定 flat/folded。
- **GUI 降級**：`_end_cap_part_spec_from_values()` 不再把 Fold Profile 預先聚合回 scalar folds；只打包 canonical scalar + raw Fold Profile，解析權回到 AE。
- **structural 路徑收斂**：factory／unknown／baseline stretched EndCap 都明確接收 resolved `x_topology`；`build_unknown_endcap_result()` 不再自行檢查 `CornerTypeId.OVERLAY`。
- **DXF 路徑收斂**：`generate_part()` 的 EndCap 不再因有無 Fold Profile 分流回 legacy exporter；統一序列化 `build_part_render_data()` 的 Final Scene。
- **BEND 覆寫修正**：只有 X profile 時只替換垂直 BEND，只有 Y profile 時只替換水平 BEND；不再因單軸 profile 把另一軸既有 BEND 全刪掉。
- **基準檔拉伸真 Bug**：舊 `get_stretched_end_cap_data()` 假設基準外框與新 CornerType 外框 polygon 頂點數／索引一致；OVERLAY 由 16 點變為不同 topology 時會 `IndexError`。已改為 X/Y 一維座標層級映射，拓撲層級數不同時保留穩定邊界／折線控制點，不做錯誤 vertex 強配；同時 metadata 與特徵映射改用 structural result 的有效 fold（OVERLAY 為 0/0）。
- **核心契約**：Head/Tail `OVERLAY + W=400` 從 AE resolver → structural material → Final Scene → DXF CUTTING X span 全程保持 `400`，且無虛構左右 X BEND。
- **TDD**：ownership 專屬測試最終 `20 passed`；包含 mixed top 拒絕、profile/scalar precedence、雙向 stale profile 衝突、unknown/baseline topology 傳遞、基準拉伸 OVERLAY 實際 synthetic DXF、Head/Tail 400 span、DXF、單軸 BEND preservation、public `generate_part()` 無 bypass。
- **回歸**：聚焦（排除 4 個硬編碼缺件 fixture）`161 passed, 2 skipped, 4 deselected`；完整原始套件 `259 passed, 2 skipped, 4 failed`，4 個 failure 全部與修改前相同，唯一原因為環境缺少 `/mnt/data/自訂.p6fold`；完整排除該 4 項後 `259 passed, 2 skipped, 4 deselected`，0 failure。
- **Bridge ownership guard**：`fold_designer_bridge.py` 不含 `ResolvedEndCapRequest(`、`PartRenderData(`、`build_part_render_data(`，維持 UI request/profile 層。
- **語法檢查**：本輪所有修改 Python 檔與新增測試 `py_compile` 通過。
- **config.ini**：未修改，SHA256 維持 `5947c847ab96a6d739d464d75f50457300ac2cd240e232eb0f2fe1d53c41683d`。

### 11:50 ProjectSession：專案 draft／committed／loaded baseline 交易所有權收斂
- **新增 deep module**：`phase6_project_session.py` 正式擁有 `project_path / loaded_baseline / committed / draft`；所有跨 interface snapshot 皆 defensive deepcopy。
- **交易 invariant**：active draft 期間 `capture_committed()` 直接拒絕，避免任何 caller 在 3D 未確定時覆寫 committed；明確 `load_project()` 可取代舊交易並清除 draft。
- **Main GUI 專案讀寫**：Load 設定 baseline + committed；Save/Save As 無 draft 時 capture main canonical snapshot，有 draft 時只能讀 `snapshot_for_save()` 的 committed。
- **3D 交易**：開啟 3D 從 committed 建隔離 draft；取消／X 丟棄；確定先套回 main GUI，再 commit canonical snapshot。ProjectSession 不同步每一個 Tk editor 值，避免新增 live mirror。
- **Bridge alternate save seam**：main-connected Fold Designer 的 `.p6fold` Save/Save As 不再自行序列化 staged workspace，統一委派 main GUI committed save；standalone bridge 保留既有 fallback。
- **active_part 導航相容**：3D Save 可保存目前既有 committed 板件的 `active_part` 導航提示，但未確定尺寸／CornerType／Fold Profile／features／板件增刪仍不得進正式存檔；draft-only 新板件不接受 hint。
- **runtime path**：`_runtime_project_path` 從 canonical committed snapshot 排除，只臨時注入 3D draft 副本。
- **TDD**：新增 `tests/test_phase6_project_session.py` 與 ProjectFile real-Tk transaction regression；包含 cancel、confirm、baseline immutable、main-connected Save 不漏 draft、active draft capture guard。
- **已知環境限制**：原始完整套件仍只有 4 個硬編碼 `/mnt/data/自訂.p6fold` 的既有缺件 failure；明確排除後為 0 failure。完整數字以 `docs/superpowers/verification/2026-08-23-phase6-project-session-verification.md` 為準。
- **config.ini**：本輪不修改，交付前再次比對 SHA256。

### 12:21 Settings 單一 Runtime 狀態來源／3D Save Defaults 交易邊界收斂
- **新增 committed settings deep module**：`phase6_settings_center.py` 新增 immutable `Phase6Settings` 與 `SettingsService`；service 正式擁有目前已確認 Runtime，snapshot crossing interface 以 defensive copy／immutable Mapping 提供。
- **Main GUI ownership**：移除 `gui.py` 的 `_settings_state`；主 GUI 收集、Tk trace、3D 確定套用、文字大小 Runtime 同步都改經 `SettingsService.update()`，AE module globals 只作 compatibility mirror。
- **持久化與 Runtime 分離**：`config.ini` 明確降級為下次啟動 persisted defaults。`SettingsService.persist_defaults()` 寫 INI 時固定不套入 AE；低階 `save_defaults_to_ini()` 新增 `apply_runtime` compatibility flag，舊 caller 預設行為仍保留。
- **真 Bug：3D「儲存為預設值」污染 AE**：修正前 main committed W=400、3D draft W=500，未確定先按「儲存為預設值」會讓 INI=500 **同時 AE.W=500**；取消 3D 後主 GUI 仍 400，但 AE 已漂移。修正後 INI 可保存 500 給下次啟動，當下 service/AE 仍 400；只有按「確定」才提交成 500。
- **Factory Defaults 不混淆**：`factory_snapshot()` 永遠來自 `ae.default_config`；「還原初始值」不受使用者 persisted defaults 或目前 Runtime 變動影響。
- **3D draft 邊界**：Fold Designer `_settings_values` 刻意保留為 transaction-local draft，不建立第二個 committed store。Cancel 維持 service/AE 原值；Confirm 才經 main GUI commit。
- **TDD**：新增 `tests/test_phase6_settings_service.py`，逐項確認 snapshot defensive、runtime update 不寫 INI、persist draft 不 commit runtime、factory immutable、重啟讀 persisted defaults、Main GUI ownership、3D Save Defaults 不污染 AE、3D Cancel/Confirm transaction。
- **聚焦回歸**：`94 passed`；`py_compile` 通過。
- **完整原始 suite**：`271 passed, 2 skipped, 4 failed`；4 個 failure 與前版完全相同，唯一原因為環境缺少硬編碼 `/mnt/data/自訂.p6fold`。
- **完整排除該 4 項後**：`271 passed, 2 skipped, 4 deselected`，0 failure。
- **config.ini**：未修改；SHA256 維持 `5947c847ab96a6d739d464d75f50457300ac2cd240e232eb0f2fe1d53c41683d`。


### 12:44 變更紀錄：GUI Project Controller 化
- **深模組 seam**：新增 `phase6_project_controller.py`；集中 `.p6fold` payload、ProjectSession ordering、read/write adapter、3D draft Save 保護與 project path 更新。
- **GUI 降責任**：`gui.py` 不再直接操作 ProjectSession 交易方法、不再直接呼叫 project read/write，也不再自行組 `schema/saved_at/final_geometry` envelope。
- **View adapter 保留**：canonical snapshot compose/apply 仍留在 GUI，沒有把 Tk widget、renderer、CornerType 或 Settings 搬進 Controller。
- **draft invariant**：active 3D Save 時 Controller 不呼叫 snapshot provider，只保存 committed；`active_part_hint` 只接受 committed 已存在板件。
- **失敗安全**：writer 丟例外時 project path 不提前切換。
- **相容性**：`app.project_session` 暫保為 `app.project_controller.session` 同物件 alias，沒有第二份 session state。
- **config.ini**：未修改。

### 12:59 Workspace Controller：主 GUI 工作區狀態所有權收斂
- **新增 deep module**：`phase6_workspace_controller.py`；集中 committed workspace 的 `existing_parts / active_part / part_profiles / box_body_profile` 與一致性規則。
- **GUI 降責任**：`gui.py` production methods 不再直接依賴 `fold_designer_part_bundle / _phase6_existing_parts / _fold_designer_last_part_key / fold_designer_box_body_profile`；四個名稱僅保留 compatibility property，沒有 shadow backing state。
- **physical presence invariant**：`box_body` 永遠存在；authoritative workspace 一旦建立，export checkbox / legacy indicator fallback 不能改寫實際板件存在性；刪除板件不刪 profile stash，重加可復用。
- **active part invariant**：目前板件必須存在；刪除 active part 時自動回退合法板件。
- **幾何 seam**：Head/Tail linked profile 仍在既有 geometry resolver 推導，Workspace Controller 只保存最終 profile stash，不 import Tk/AE。
- **真 Bug：跨專案 stale Fold Chain**：載入新專案若 `box_body_profile=None`，舊版 `_store_fold_designer_workspace()` 不會清掉上一個專案 Fold Chain；現在明確區分「欄位存在且 None = 清除」與「欄位不存在 = partial update 保留」。
- **測試 fixture 對齊**：使用 `object.__new__(BoxCalculatorGUI)` / `SimpleNamespace` 的半初始化測試改為明確注入 `Phase6WorkspaceController`，production 不新增缺 Controller 時退回 legacy state 的後門。
- **原始完整 suite**：`293 passed, 2 skipped, 4 failed`；4 個 failure 仍全為環境缺 `/mnt/data/自訂.p6fold`。
- **排除 4 個既知外部 fixture**：`293 passed, 2 skipped, 4 deselected`，0 failure。
- **config.ini**：未修改，SHA256 仍為 `5947c847ab96a6d739d464d75f50457300ac2cd240e232eb0f2fe1d53c41683d`。

### 13:20 Fold Designer Bridge 瘦身：EndCap semantics／Fold Profile 正式分家
- **EndCap semantics owner**：新增 `phase6_endcap_semantics.py`，集中 assembly type、Head/Tail FW follow/override、CornerType raw codec；`gui.py` 不再從 Bridge 取得這些機械語意。
- **Fold Profile owner**：新增 `phase6_fold_profiles.py`，集中 BoxBody/EndCap profile build/read/merge、linked mating chain、outside-dimension compensation、profile → `FoldProfileSegment`。
- **Bridge 降責任**：`fold_designer_bridge.py` 從 5391 行降到 4685 行；保留 Tk/Fold Designer transaction、scene/view adapter 與舊 caller compatibility re-export。`_phase6_resolve_profile_key()` 維持在 Bridge，避免把 Notebook/renderer label adapter 誤塞進 domain module。
- **Production caller ownership**：`gui.py` 現在從 `fold_designer_bridge` 只 import `Phase6FoldDesignerApp`；EndCap/Fold domain functions 直接 import owner module。
- **相容性**：Bridge 舊函式名稱是 owner function 的同一函式物件，不是 wrapper，既有 caller/test 不需一次性破壞。
- **回歸**：聚焦 `160 passed, 2 skipped, 4 deselected, 0 failure`；完整原始 suite `299 passed, 2 skipped, 4 failed`，4 個仍全為 `/mnt/data/自訂.p6fold` 缺件；排除後 `299 passed, 2 skipped, 4 deselected, 0 failure`。
- **config.ini**：未修改。

### 13:45 FinalScene 3D View 深模組化
- 新增 `phase6_final_scene_view.py`，集中 profile folding、Final material triangulation、mesh boundary、BEND/MARKING/BLIND_HOLE 投影、operator 尺寸圖形、3D viewport 與 scroll zoom。
- `fold_designer_bridge.py` 保留 FinalScene query、active Fold Profile adapter、operator finished dimension 數值解析；以 `FinalSceneViewRequest` 把已解析資料交給 View。
- View 直接消費 `PartRenderData.material / scene / fold_guides`，不呼叫 manufacturing build、不重建 CUTTING material、不從 scene 重推 manufacturing fold guides、不解析 CornerType。
- compatibility `_phase6_zoom_scale / _phase6_last_cutting_mesh / material / error` 改為 `Phase6FoldDesignerApp` property，backing state 只在 `final_scene_view`。
- Bridge 由 4685 行降至 4118 行，減少 567 行；重點為 renderer/view implementation ownership 移出 Adapter，而不是追求行數。
- 聚焦 3D 回歸 `87 passed`；完整原始 suite `303 passed, 2 skipped, 4 failed`，4 個仍全為 `/mnt/data/自訂.p6fold` 缺件；排除後 `304 passed, 2 skipped, 4 deselected, 0 failure`。
- `config.ini` 未修改。

### 14:03 診斷快照聚合深模組化
- **新增 deep module**：`phase6_diagnostics.py`，集中 `phase6-fold-diagnostic-v1` schema、JSON-safe、DrawingScene primitive、Shapely material、FoldGuide 序列化與 UTF-8 diagnostic writer。
- **active diagnostic**：Bridge 只組 `DiagnosticSnapshotContext` 與 active manufacturing provider；render 失敗維持 snapshot 可保存，只寫 `render_error`。
- **all-part diagnostics**：`.p6fold.final_geometry` 改用 `collect_final_geometry_diagnostics()`；單一板件 render error 只污染該 row，其他板件仍完整輸出。
- **Bridge 降責任**：舊 `_phase6_json_safe / _phase6_serialize_scene / _phase6_material_diagnostic / _phase6_serialize_fold_guides / _phase6_write_diagnostic_json` 改成 Diagnostics 的 direct re-export，不留第二套 serializer。
- **所有權**：`_phase6_collect_workspace_state()`、manufacturing query payload、`.p6fold` reloadable snapshot、Tk filedialog 仍留在 Bridge；Diagnostics 不依賴 Tk／ProjectSession／SettingsService／manufacturing build。
- **回歸**：完整原始 suite `311 passed, 2 skipped, 4 failed`；4 個仍全為 `/mnt/data/自訂.p6fold` 缺件。排除既知 4 項後 `311 passed, 2 skipped, 4 deselected, 0 failure`。
- **config.ini**：未修改。

## Phase6 通用設定面板深模組化
- 新增 `phase6_settings_panel.py`，集中 schema 驅動的通用 Tk 設定 UI、page cache、advanced 顯示、baseline lazy data 與左側全域 W/H/D/T/STOCK/文字大小 controls。
- `fold_designer_bridge.py` 保留 `_settings_values` 3D draft、debounce/profile rebuild、CornerType/assembly/EndCap FW domain editor 與 transaction；domain controls 透過 extension seam 插入 Panel。
- Bridge 由 4009 行降至 3729 行；Panel 不 import AE/Project/FinalScene，也不保存 `_settings_values`。
- 新增 `tests/test_phase6_settings_panel_ownership.py`；完整可執行 suite `322 passed, 2 skipped, 4 deselected, 0 failure`。

## 3D Designer 草稿工作區所有權收斂

- 新增 `phase6_designer_workspace.py`，集中管理 3D Fold Designer 的板件 presence、active/selected、profile/feature stash、dirty 與 switching 生命週期。
- `fold_designer_bridge.py` 的 legacy workspace 欄位改成同一 owner 的 compatibility property，不再保存第二份 backing state。
- `select / home / save / switch / add / remove` production lifecycle 改走 Workspace API；切換時固定先保存 outgoing draft，再開始 target switch。
- Project snapshot、Diagnostics、Scene query 與 FinalScene request 的 active/presence/profile/features 改由同一 Workspace owner 取得。
- 刪除非箱身板件保留 profile/feature stash，重新加入可恢復；`box_body` 保持 mandatory。
- 半初始化測試 fixture 全面補上正式 `Phase6DesignerWorkspace`，production 沒有加入缺 owner 時回退舊 dict 的相容後門。
- 不修改 `config.ini`、不升版 `.p6fold` schema、不改 UI 操作方式、不搬 CornerType／linked EndCap／孔位座標幾何到 Workspace。

## 15:40 — 統一開孔編輯器交易狀態機收斂

- 新增 `phase6_hole_editor_session.py`：以純 Python `Phase6HoleEditorSession` 集中管理多 context feature list、selection、active edit、Undo、Confirm/Cancel。
- `_open_unified_hole_editor()` 移除 `selected / active_snapshot / undo_history_ref / context_feature_lists / context_original_features` 等本地交易 backing state。
- 插入、拖曳、旋轉、reference edit、process toggle、delete、Undo、圓孔排列 preview、context switch、Confirm/Cancel 全部改走 Session action。
- 圓孔排列開始 preview 前會先提交先前 transient edit，避免舊 `seed_snapshot` 與 active edit 形成兩套 Undo/Cancel 語意。
- GUI ownership guard 鎖住：統一開孔編輯器不得直接 `append/del/item assignment` 修改 `feature_list`。
- 新增真 Tk 回歸：實際執行「選取 → 刪除 → Undo → 再刪除 → 取消全部」，確認 original feature 正確恢復。
- 原始完整 suite：`348 passed, 2 skipped, 4 failed`；4 項均為既知 `/mnt/data/自訂.p6fold` 缺件。
- 最終新增 ownership guard 後，排除既知 4 項完整 suite：`349 passed, 2 skipped, 4 deselected, 0 failure`。


## 17:30 箱身多結構型態：W 二分／W 三分／側背分離

- 新增正式箱身結構 state owner，支援一體成型、二件式（W 二分）、三件式（W 三分）、三件式（側背分離），各型態 configuration 獨立保留。
- 二件式只拆 W，左右包外雙向補全、最小 50 mm；中央一般 90° BEND 預設 12 mm、最小 12 mm，>=50 mm 只警告不阻擋。
- 新增封頭／封尾端部十字截角：各自使用實際 ybottom1，共用可調 +5 mm 與 0.5T 單邊留肉。
- 新增底板實際 seam 交會判定與局部十字避讓：預設總長 20 mm、0.5T 留肉；W 三分預設 50 mm seam 可自然避開 55 mm shrink。
- W 三分預設 `50 / (W-100) / 50`；左右連動，中間可直接輸入。
- 側背分離：左右側板 D 成型尺寸、後側 15 mm 折邊、後面板寬 `W - compensation`（預設 0.5T）。
- 多件式正式接入 FinalScene：2D exploded preview、3D 組裝、獨立 DXF export 共用同一 resolved pieces；跨 seam 開孔不移孔。
- 箱身頁 GUI 預設鎖定且隱藏細項；解鎖後顯示型態專屬欄位；截角／避讓預設收合；側背分離新增唯讀 D 顯示。
- TDD 專項：`36 passed`。
- 完整回歸排除 4 個既知 `/mnt/data/自訂.p6fold` 外部 fixture：`381 passed, 2 skipped, 4 deselected, 0 failed`。原始 160603 基準包單跑該 4 項亦為相同缺檔 failure。
- `python -m compileall -q .` 通過。
- `config.ini` 未修改，SHA256 維持 `5947c847ab96a6d739d464d75f50457300ac2cd240e232eb0f2fe1d53c41683d`。

## 18:36 箱身多結構 code-audit hardening

- 以 `PHASE6_HOLE_EDITOR_CANVAS_VIEW_FULL_20260823_160603 (1).zip` 為唯一基準，採納 `PHASE6_BOX_BODY_SPEC_CODE_AUDITED_20260823_165819.zip` 的程式查核契約，不回套舊交付包。
- **側背分離 3D**：修正右側板後折鏡射與 assembly transform；組裝後 W/D envelope 不再向箱外擴張。
- **operation-aware clipping**：跨 W seam 的 CUTTING／BLIND_HOLE／MARKING／DATUM／layered profile 依每片實際 material boundary 裁切並保留 process layer；完全位於單片內的 CIRCLE/RECT 維持原 primitive。
- **strict resolver**：W 二分保存總和不等於 W、W 三分左右不一致或總和錯誤時 fail closed，不再 silent repair corrupt/direct-API state。
- **正常全域 W 編輯**：另在 GUI settings commit seam 依最後 driver 重算 W 分件；新 W 無法形成合法分件時 reject/revert，resolver 不因此放寬。
- **Legacy migration**：舊 `.p6fold` 沒有 structure state 時保持一體成型；缺 structure lock 時已知型號 locked、`自訂`／legacy `未知類型` unlocked；新檔 explicit 保存值永遠優先。
- **Corner canonical**：封頭尾／底板十字截角的 0.5T 單邊留肉量改由既有 `CROSS + RETAIN + CornerDirection + amount_t` 解析，不另造 T 公式。
- **TDD**：audit contract + 箱身結構真 Tk 專項 `38 passed`。
- **完整原始 suite**：`395 passed, 2 skipped, 4 failed`；4 個 failure 仍全部為環境缺少硬編碼 `/mnt/data/自訂.p6fold`。
- **排除既知 4 個外部 fixture**：`395 passed, 2 skipped, 4 deselected, 0 failure`。
- `python -m compileall -q .` 通過。
- `config.ini` 未修改，SHA256 維持 `5947c847ab96a6d739d464d75f50457300ac2cd240e232eb0f2fe1d53c41683d`。

## 21:21 FW 連動亂跳修復：控制權狀態機＋語意鏡像

- **根因 1：對稱折彎以 raw row index 鏡像。** 單側曾增刪額外折後，`fw_left` 可能錯連到 `d_right / Z` 等其他欄位。
- **修正：** 箱身既有結構列改用 `phase6_key` 語意配對；FW/D/Z 長度與折角邊界都不再用位置猜測。單側歷史留下的無 key 額外折若對面是結構列，禁止位置式鏡像，避免再污染結構欄位。
- **根因 2：封頭／封尾 FW 只有兩個 `follow_box` 布林值，無法表達實際操作控制權。**
- **新 FW 四態：** `FOLLOW_BODY / FOLLOW_HEAD / FOLLOW_TAIL / INDEPENDENT`。
  - 初始：箱身 FW 同時控制封頭、封尾。
  - 先改封頭：封尾跟封頭；持續改封頭仍同步。
  - 先改封尾：封頭跟封尾；持續改封尾仍同步。
  - 再手動改原 follower：兩端解除連動，各自獨立。
  - 任意狀態下，使用者重新提交箱身 FW（即使同值）即重新接管兩端。
- **UI：** 封頭／封尾 FW 欄位保持可直接數字輸入，不再靠 disabled/readonly 維持一致性；舊「跟隨箱身 FW」勾選只作相容／狀態顯示，不再作主要控制入口。
- **3D：** Fold Designer 內修改箱身 FW 也會先讓箱身重新接管，再重建 linked EndCaps；封頭／封尾直接修改走同一四態狀態機。
- **存檔：** `.p6fold` 保存 `endcap_fw.mode` 與兩端值；舊 per-part `follow_box` 專案載入時保持既有效值，不猜測 Head/Tail 主導者。
- **防呆：** 封頭／封尾 FW 自由輸入若暫時輸入非數字，不改控制權並回復目前有效值。
- **TDD：** 新增狀態機、主 GUI、3D box takeover、語意 FW/angle mirror、無 key 額外折污染防護與 `.p6fold` round-trip 回歸。
- `config.ini` 未修改，SHA256 維持 `5947c847ab96a6d739d464d75f50457300ac2cd240e232eb0f2fe1d53c41683d`。
