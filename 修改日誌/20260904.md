# 2026-09-04 修改日誌

## Release 封包污染修正
- 重驗 `PHASE6_BOXBODY_ENDCAP_DOOR_RECEIVING_FULL_20260904_092052.zip` / UPDATE，確認 `.git/**` 270 entries、`.scratch/**` 69 entries 被誤收。
- 先同步 `phase6-release-packaging` Skill 與全域踩坑庫，將 `.git/.scratch` 定義為 FULL/UPDATE 共同禁止的封包根目錄。
- 已以 TDD 補上 release policy/collector/ZIP verifier regression；最終重包批次時間戳 `20260904_120434`，交付前再以 fresh extraction 驗證 CRC/SHA、`config.ini`、`.git/.scratch` entry policy 與 canonical baseline+UPDATE parity。

## Release baseline 去硬編碼
- 修正 `release_required_artifacts.json` 將 UPDATE baseline 具體 FULL 檔名永久寫死的設計缺陷；schema 升為 v2，改為 `update_baseline_mode: explicit_runtime_archive`。
- `tools/phase6_release.py` 改由每輪 `build_release(..., baseline_archive=...)` runtime 參數提供 baseline，只驗 ZIP 存在性/格式/CRC，不再以 manifest 比對時間戳檔名。
- `phase6-release-packaging` Skill 同步禁止任何具體時間戳 FULL baseline；baseline 未明確提供或 provenance 不明時 fail closed，不得搜尋「最新」或拿歷史包頂替。
- 新增 policy regression：manifest 禁止 `canonical_update_baseline_archive`，不同名稱的合法 FULL ZIP 均可作 runtime baseline。
