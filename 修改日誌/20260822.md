# 2026-08-22

## 3D Final Part Geometry direct render

- 移除 Phase6 3D 對 legacy Renderer 的執行與 fallback。
- 移除 3D 由 scene BEND 二次反推 fold profile 的路徑。
- 3D 直接折疊 authoritative PartRenderData.material，折向/角度使用 Fold Designer 現有 X/Y profile。
- Final geometry 失敗時顯示錯誤，不再顯示另一套舊模型。
- display mesh 採 0.05 mm topology-preserving simplify，降低高密度圓弧 triangulation 成本，不修改 authoritative material。
- 新增 3 個 regression tests。

## 封尾 native orientation / 門把 baseline / 折彎診斷存檔

- 修正 head/tail 只應共用製造尺寸，不應直接複製同一 Y profile；封尾改用 native `ybottom1 -> D core -> FW -> ytop1`。
- 標準四段的舊封尾工作區會安全遷移；含額外自訂折段時不強制重排。
- 門基準 LWPOLYLINE 不再以 `len(points) > 10` 判定舊外框；只排除真的跨整張板材 bounds 的結構輪廓。
- 門基準 `INSERT` 會展開 virtual entities，再按原 operation layer 規則映射，避免 block 內把手 CUTTING 消失。
- 3D 設定 footer 新增「存檔」，輸出 `phase6-fold-diagnostic-v1` JSON；不等同主 GUI 確定。
- JSON 保存全部 fold profiles / settings / CornerType / active payload，以及目前 Final Part Geometry scene/material。
- 新增 `tests/test_phase6_tail_native_orientation_and_save.py`。
- 完整 Xvfb pytest：109 passed。

## 3D core 主面 / 有限 BEND ownership / 操作尺寸 / `.p6fold` 專案檔

- 3D 折疊主面不再用 profile 中間索引猜測；改以 segment 的 `core` 語意選擇主面。箱身多個 core 取中央 W core，封頭/封尾均以 `endcap_d_core` 為主面。
- `PartRenderData` 新增 `fold_guides`；由 final scene 的 BEND LinePrimitive 產生有限長度 FoldGuide，包含 axis / position / span_start / span_end。
- 3D 每一道折只作用於實際 BEND 線覆蓋範圍；留肉若位於某條 BEND span 外，只略過該一道折，後續有效折彎仍正常套用。
- 使用使用者提供的封頭/封尾診斷 JSON 驗證：修正後兩者外部折疊 span 均為 392 × 244 × 25，差值為 0；原生 CUTTING/孔位差異保留。
- 3D 關閉 X/Y/Z 軸、ticks、grid、pane，改顯示操作員成品尺寸與折邊摘要；門顯示成品 335 × 535，而非引擎補償 core 331 × 531。
- 預設相機改為 W 水平、H 垂直的操作視角，避免門看起來寬高顛倒；使用者旋轉後維持其視角。
- 3D 開窗不再重新對整個主 GUI 執行 TextScaleController.apply()。
- 第一個板件預覽先完成 Tk/Matplotlib viewport resize，再進行 authoritative model render；抑制 resize 的空/舊畫面 repaint，使首次選門由 3 次 draw 降為 1 次。
- 同環境實測：上一包 open 0.2477 s / activate door 0.2056 s / draw 3；本版 open 0.1306 s / activate door 0.0742 s / draw 1。
- 新增 `phase6_project_file.py` 與 `.p6fold` 專案格式（UTF-8 JSON，schema `phase6-fold-project-v1`）。一次保存全域設定、全部現有板件、全部 X/Y profiles、CornerType、features、face features、workspace，以及每片 authoritative scene/material/fold_guides 診斷資料。
- 主 GUI 可從 `.p6fold` 還原工作區並自動開啟存檔時 active part 的 3D 工作區。
- Windows 使用 HKCU `Software\\Classes` 註冊 `.p6fold` 開啟命令，不需管理員權限；frozen EXE 使用 `"<exe>" "%1"`，script 模式使用 `"<python>" "<gui.py>" "%1"`。
- 使用使用者閉合後的門診斷 JSON 驗證 material `interior_count=7`，門把已成為 material 真孔。
- 新增 `tests/test_phase6_core_fold_ownership.py`、`tests/test_phase6_3d_operator_view.py`、`tests/test_phase6_project_file.py`。
- 完整 Xvfb pytest：124 passed。
- `fold_designer_original.py` SHA256 維持 `f30b5e790efe2b4bd91d689735ce4fd2dd8f59f276c299f71043ef8ee93cb4de`。

## 箱身 Fold Chain / 封頭尾連動 / 2D handoff / 板件刪除

- `BoxBodyPartSpec` 新增 authoritative arbitrary `fold_profile`，manufacturing box scene/BEND/material 改直接使用目前 Fold Chain；3D 中新增/刪除折段按「確定」後真正進入主 GUI 2D，不再被舊 9 段公式重建。
- `build_linked_endcap_xy_profiles()` 以 D-W-D semantic core 與外側 ordered chain 推導封頭/封尾 mating 折法；不依賴固定段數，回歸涵蓋 3/5/9/12/20 段。
- `.p6fold` 載入與 3D transaction commit 都以箱身 Fold Chain 為 source of truth，舊 head/tail profile 只作診斷/暫存，不覆蓋 derived mating topology。
- 封尾維持 native orientation；封頭/封尾 2D 與 DXF export 改由相同 Final Scene 的 BEND primitives 輸出。
- 修正自訂/無 baseline 封頭尾 structural outline 未 closed，material resolver 可能把小 CUTTING feature 認成主材料的問題。
- 已知型號箱身在 arbitrary Fold Chain 下仍保留基準固定孔/標記；基準 feature 以 face-local 座標映射到新 D/W/D 核心。
- 主 GUI 箱身結果欄與 face hit-zone 改讀 authoritative Fold Chain；使用上傳 `自訂.p6fold` 驗證 5 段箱身結果為 938.00 × 596.00 mm。
- `existing_parts` 正式控制主 GUI 2D/export presence；箱身不可刪除，其他板件可刪除/加回，2D tab 與 export flag 隨 transaction 同步。
- 刪除非 linked 板件只移除 presence，不清除 stashed local profile；重新加入可恢復原折法。
- 修正 derived 封頭/封尾重新加入後再次「確定」會套用 legacy 四鍵 Y parser 並失敗；任意 linked Y profile 現只保留 end-cap 獨立 X flange 值，Y 永遠重新由箱身 Fold Chain 推導。
- 20 段端蓋 mating span 使用總量守恆整數配額，避免逐段 round 造成 blank size 漂移。
- 新增 `tests/test_phase6_linked_fold_chain_and_parts.py`，包含使用者實際 `自訂.p6fold` 回歸。
- 最終乾淨 ZIP 解壓驗證：149 passed；上傳 `自訂.p6fold` 實檔 delete/add-back transaction smoke 通過。


## 2D/3D 共用 FinalScene / 組合方式 / FW override / 折後包外

- 新增箱體層級 assembly type：INSERT / OVERLAY / INSERT_OVERLAY；同一狀態驅動封頭/封尾上方 CornerType 與箱身 finished-height occupancy。
- 修正 FW 語意：FW 是 Frame Width，不可被 arbitrary fold allocator 合併。5 段 linked end-cap 改為 Head `16 -> FW25 -> D244 -> 15`、Tail native `15 -> D244 -> FW25 -> 16`。
- 封頭/封尾 FW 預設跟隨箱身但可各自解除連動；override 不反寫 global FW，並保存/還原於 `.p6fold`。
- 主 2D 與 Phase6 3D 共用 `_authoritative_render_data` cache；相同 PartSpec/context 返回同一 `PartRenderData`。
- DXF export 新增 `save_part_render_data_dxf()`，直接序列化既有 FinalScene，不再由 GUI 再跑 `generate_part()`。
- 3D 尺寸改量實際折後 triangles 的 outside envelope；`自訂(1).p6fold` 箱身實測 400 × 596 × 250，2D 展開仍為 938 × 596。
- 板件 selection 與 activation 分離；optional part 可選中後直接刪除/Delete，不必先進編輯頁。
- 完整工作目錄 Xvfb pytest：173 passed；實檔 smoke 驗證同一 box-body state 的 2D→3D→DXF manufacturing build 次數為 1。

## Phase6 封頭尾跟隨箱身刪折與 90 度修正
- 修正 5 段 `FW-D-W-D-FW` 箱身時，封頭/封尾仍被硬補 `ytop1` 外側折的錯誤。箱身外側折不存在時，封頭/封尾對應折現在會自動刪除。
- EndCap 2D blank 尺寸改由同一份 Fold Profile 聚合 legacy structural scalar；5 段案例由 422×300 正確縮為 422×284，BEND 由 5 條降為 4 條。
- `read_endcap_xy_profiles()` 不再要求 `ytop1` 必須存在；缺少代表 0，任意額外 mating rows 僅聚合供 legacy adapter 使用，完整 BEND 仍由 Fold Profile authoritative。
- 修正 3D payload 因缺 `ytop1` 讀取失敗而退回舊 300/5-BEND geometry 的問題；2D/3D 現在使用同一 EndCap PartSpec/FinalScene。
- `自訂(2).p6fold` 實檔：Head/ Tail 均為 422×284、4 BEND；Y 相鄰折後夾角 90° / 90°。
