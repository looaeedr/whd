# 2026-08-29 修改日誌

## 組合圖 Joint 診斷雜訊與 UPDATE 漏包修正（更正版）

### 根因
1. WRAP / Joint Registry 完成版把開發診斷資料直接接進正式組合圖，新增了 Joint selector、Registry 長狀態字串與診斷 overlay。
2. `金庫型.p6fold` 的 4 筆 `LEGACY_MIGRATED` Joint 是內部組合語意，不是板件，也不應成為操作員圖層。
3. 前一個 21:11 UPDATE 又錯用「上一版 FULL」做窄差異，只帶本輪少量檔案；若套在使用者既定基準上，就會缺少前面累積功能／依賴。
4. Door / Base Plate 單板若被強制拖進整櫃 assembly solve，會破壞單板 3D canonical FinalScene gate；因此此真值鏈修正保留，但與 Joint 畫面雜訊分開驗證。

### 更正修正
- 正式組合圖不傳 `joint_diagnostics` / `selected_joint_id`，Joint/WRAP 診斷仍留在 canonical resolved geometry。
- 移除本輪新加的正式操作畫面 Joint debug selector 與 debug status suffix，恢復到新增 Joint debug 前的操作 layout。
- `_phase6_query_final_render_data()`：箱身／封頭尾／USER_ADDED Joint 參與板件走 resolved assembly；Door / Base Plate 等獨立板件直接讀自身 canonical manufacturing FinalScene。撤回此修正會讓單板 3D gate 重現既有失敗，因此列為正式回歸修正。
- UPDATE 改為相對唯一累積基準 `PHASE6_FW_LINK_BUGFIX_FULL_20260823_212355(3).zip` 收集全部差異。
- `個人AI檔案庫/**` 每次 UPDATE 強制全收，即使未變；`config.ini` 永遠禁止進 UPDATE；`BACKUP/**` 與快取不進 UPDATE。
- 新增 `.agents/skills/engineering/phase6-release-packaging/SKILL.md` 與 machine-readable release policy，讓出包器自動 enforce，不再靠手工記憶。

### 防退化
- `test_public_assembly_adapter_keeps_joint_diagnostics_out_of_operator_scene`：正式組合 bundle 不得帶 Joint debug layer。
- `test_operator_assembly_diagnostics_does_not_expose_joint_debug_selector`：正式操作 layout 不得再長出本輪新加的 Joint selector。
- `test_phase6_release_packaging_policy.py`：鎖住累積基準、AI 全樹、禁止 `config.ini`。


## EndCap/Tail 單級 INSERT 上方截角 38×27 校正

### 使用者回報
- 同一封頭／封尾：單板 3D 顯示 `40×27`，組合圖／2D 顯示約 `39×27`；使用者確認正確製造尺寸是 `38×27`。

### 根因
1. 單板 3D 某些路徑會因 canonical relief source 驗證失效而退回 legacy fixed relief，形成 `40×27`。
2. backprojection solver 將 ±T/2 physical skins 的正常 mating contact 帶納入 crossing envelope，取到外側約 `38.98`，形成 `39×27`。
3. 真實折後 mating boundary 投回 EndCap flat UV 為 `38.00`；contact 不是 penetration。

### 程式修正
- `ae_engine/assembly_collision.py`
  - 單級 `INSERT` 維持單級 topology；迭代只收斂 primary 尺寸。
  - 排除正常 skin contact band，以實際 mating boundary 取得 relief。
  - 不對 `INSERT_OVERLAY` 等真正二級 topology 套單級規則。
- `fold_designer_bridge.py` / `gui.py` / `phase6_final_scene_view.py`
  - 延續上一輪 canonical 2D/3D/assembly 尺寸同步與板件 visibility UI。
  - 單板 3D、組合圖、2D 尺寸均消費 authoritative final material。
- 測試新增／更新：
  - `tests/test_assembly_collision_integration.py`
  - `tests/test_phase6_assembly_3d_view.py`
  - `tests/test_phase6_corner_dimension_controls.py`
  - `tests/test_phase6_latest_layout_contract.py`
  - `tests/test_phase6_return_2d_corner.py`

### 實檔驗證
`自訂(9).p6fold`：
- Head/Tail `verified=True`，`errors={}`。
- Head 左右角 = `38×27`；Tail 左右角 = `38×27`。
- 無 secondary stage。
- 組合診斷 = `封頭：38×27；封尾：38×27`。
- main 2D vs assembly material symmetric difference：Head `0.0`、Tail `0.0`。

### Focused regression
`46 passed / 0 failed`。

## 文件缺漏修正
上一個 `002730` 交付包雖已有程式／測試修正，但文件未跟同版更新，違反個人 AI 檔案庫的「程式修改即同步文件」規則。本輪把文件同步列為交付 completion gate，並將此失誤寫入踩坑庫，避免再犯。

## 組合碰撞顯示／OVERLAY／Registry 驗收閘門修正

### 使用者回報
- 組合圖顯示「未偵測到碰撞」，但實際 assembly solver 已經有切除結果。
- `INSERT_OVERLAY` 與 `OVERLAY` 都可遇到同類問題；不能再逐一由使用者人工試每一種組合。

### 根因
1. 組合圖的碰撞 overlay 拿「已完成 relief 的 final material」再做碰撞診斷，因此求解前真正的碰撞證據已被切掉，UI 會誤顯示 0 段。
2. `projection_has_material_penetration()` 對 inward tolerance 後已完全消失的微小薄片又 fallback 回原 material，會把純數值薄片重新判成穿透。
3. `OVERLAY` X profile 為 flat 時曾跳過 corner topology normalization，triangle-skin 交線會把合法單級角解成 `25×11.332 + 24.231×0.15` 之類假二級。
4. 對稱幾何的左右 raw crossing 可能因三角化漏採樣變成不同尺寸；不能把 tessellation 非對稱當成機械非對稱。

### 程式修正
- `fold_designer_bridge.py`
  - 組合 bundle 額外保存 `pre_solve_probe_parts`；display material 與 collision probe material 分離。
- `phase6_final_scene_view.py`
  - 組合圖紅色碰撞區改用求解前 EndCap probe，不再拿已切完的 final material 假裝重新偵測。
- `ae_engine/assembly_collision.py`
  - tolerance inward-buffer 已消失即視為 sub-tolerance sliver，不得復活原 material。
  - final refold 對 topology-normalized edge 只有同時符合「邊界帶 <= topology tolerance」且 semantic mid-surface 無 penetration 才可判為合法 contact。
  - 所有 assembly intent 都維持原合法 corner topology stage count；3D 只決定尺寸，不能憑空新增／刪除級數。
  - 若 Box Body X、EndCap X 與左右 corner component 都真正鏡像對稱，左右 collision evidence 取 canonical mirror union，消除 triangle tessellation 漏採樣；非對稱 profile 不強制對稱。

### Registry 驗收閘門
- 新增 `tests/test_phase6_assembly_intent_registry_matrix.py`：直接 parametrized `BOX_ASSEMBLY_TYPE_IDS`。
- 新增 `tests/test_phase6_assembly_registry_gui_matrix.py`：每個已註冊 assembly intent 必須自動驗證：
  - Head + Tail 都有求解前 collision evidence；
  - 求解後 `verified=True`；
  - corner topology stage count 不被 solver 改寫；
  - 對稱測件左右結果必須 mirror-equal；
  - 2D / 單板 3D / 組合圖 material 一致；
  - collision overlay 實際有紅區段；
  - Save / Reload 後 material 不變。
- 未來新增新的 `BOX_ASSEMBLY_TYPE_IDS` 項目會自動增加測試案例；沒有通過 registry gate 不得交付。

### 實檔驗證
- `自訂(9).p6fold` / INSERT：Head/Tail verified；碰撞顯示 170 段；38×27 保持；2D=single3D=assembly；reload diff=0。
- `自訂(10).p6fold` / INSERT_OVERLAY：Head/Tail verified；碰撞顯示 198 段；`40×23 + 16×4` 保持；2D=single3D=assembly；reload diff=0。
- OVERLAY 對稱 GUI 測件：Head/Tail verified；碰撞顯示 122 段；假二級已消失；左右單級尺寸一致為 `25×11.482`（此數字僅為該測件 3D regression evidence）。

### 驗證結果
- collision / backprojection / registry core：`80 passed, 2 skipped`。
- registry GUI matrix：`3 passed`（INSERT / OVERLAY / INSERT_OVERLAY）。
- shared assembly + dimension：`33 passed`。
- corner dimension controls：`7 passed`。
- return-to-2D：`1 passed`。
- 3D cutting mesh：`8 passed`。
- 3D view regressions：`5 passed`。
- 專案舊完整 tests 仍含既有 stale tests；已抽查 `test_gui_production_imports_ae_engine_not_legacy_core_modules` 與舊 corner-lock test，在 `064513` 基準包同樣失敗，未為湊綠燈而修改既有正確 UI/幾何。


### 最終封包
- FULL：`PHASE6_ASSEMBLY_COLLISION_REGISTRY_GATE_FULL_20260829_075946.zip`
- UPDATE：`PHASE6_ASSEMBLY_COLLISION_REGISTRY_GATE_UPDATE_20260829_075946.zip`
- Fresh gate：core `80 passed, 2 skipped`；dimension/3D `51 passed, 2 skipped`；return-to-2D `1 passed`；registry GUI 三種 intent 獨立 xvfb smoke 全部 PASS。
- UPDATE 僅收錄相對 `064513` 基準差異檔，且不得包含 `config.ini`。

### [07:56] 已認證截角資料庫第一階段實作
- 新增 `ae_engine/certified_relief_registry.py`，建立 CERTIFIED / PROVISIONAL_3D / CERTIFIED_FROM_3D / ENGINE_CONFLICT / FAILED 信任狀態。
- 新增首筆已認證規則 `ENDCAP_TOP_INSERT_STRUCTURAL_CONTACT_V1`。
- 規則保存公式，不保存單一輸出尺寸：`X = EndCap side fold + (effective FW - T)`、`Y = EndCap FW + INSERT amount_t*T`。
- 限定適用於已確認的 linked-FW INSERT 拓撲；仍有獨立 `ytop1` row 的拓撲不套用本公式，避免把 38×27 錯套到未認證幾何。
- `solve_world_backprojected_endcap_relief()` 新增 `assembly_intent`，先查 CERTIFIED registry；命中後 3D 只做 shadow validation，不得覆蓋公式結果。
- `fold_designer_bridge.py` 已把目前組合方式傳入 solver。
- 新增 `tests/test_certified_relief_registry.py`，並通過相關 user8/user10/registry 回歸。

## [12:50+] 已認證截角資料庫完整化

### 架構完成
- `ae_engine/certified_relief_registry.py` 升級為正式 Certified Relief Registry owner。
- 金庫型固定 EndCap / Door / Indicator Box / Indicator Door / Base Plate 皆由 family-aware registry 供應。
- 受電箱固定 EndCap / Door / Indicator Box / Indicator Door / Base Plate 明列 family-specific rule，禁止靜默借用金庫型。
- Assembly Intent active formulas：
  - `ENDCAP_TOP_INSERT_STRUCTURAL_CONTACT_V1`：linked-FW INSERT；`自訂(9)` 38×27 regression。
  - `ENDCAP_TOP_INSERT_STANDARD_V1`：標準 `ytop1` INSERT。
  - `ENDCAP_TOP_OVERLAY_STANDARD_V1`：flat-X 標準 OVERLAY。
  - `ENDCAP_TOP_INSERT_OVERLAY_STANDARD_V1`：標準二級 INSERT_OVERLAY。
  - `ENDCAP_TOP_INSERT_OVERLAY_LINKED_FW_V1`：linked-FW 二級 INSERT_OVERLAY，`CERTIFIED`；公式為 `primary_u=side_fold+FW`、`primary_v=FW-1T`、`secondary_u=side_fold+0.5T`、`secondary_depth=2T`，沒有硬寫 16/23/14/4。

### 安全契約
- CERTIFIED / CERTIFIED_FROM_3D 命中後，3D 僅 shadow validate；`ENGINE_CONFLICT` 不得改 canonical formula。
- fallback 開關改為「未知組合允許3D求截角」；關閉時仍必須查 certified registry。
- `REGISTRY_AMBIGUOUS` 直接阻止自動決策，禁止吞例外後改走 3D。
- Save/Reload 保存 `rule_id / rule_revision / trust_level / canonical_accepted / shadow_validation`；stale revision 不 replay。
- Promotion UI 只建立 `PROMOTION_CANDIDATE` manifest，`mutates_registry=False`，不能 runtime 一鍵寫入正式 registry。
- Head 2D canonical scene 最後做 Y mirror，所以 semantic TOP joint 在 Head physical bottom；Tail 保持 native，semantic TOP 在 physical top。Registry evaluator 必須依此放置 cut。

### 自動驗收
- `tests/test_phase6_assembly_registry_gui_matrix.py` 改由 active Certified Relief Registry 自動列舉 Assembly Intent，不再依手工白名單。
- 每筆 active registry rule 自動驗證 ID/revision 唯一、evidence/公式存在、fixed policy 四角完整。
- 新 intent / new active rule 若未通過 registry-driven matrix，不得視為可交付。

### 實檔
- `自訂(9).p6fold`：Head/Tail `ENDCAP_TOP_INSERT_STRUCTURAL_CONTACT_V1@1`，`CERTIFIED`，38×27，collision 170 segments，2D=single3D=assembly，reload diff=0。
- `自訂(10).p6fold`：Head/Tail `ENDCAP_TOP_INSERT_OVERLAY_LINKED_FW_V1@1`，`CERTIFIED`，40×23 + 16×4，collision 198 segments，2D=single3D=assembly，reload diff=0。


## [13:39] Certified Relief Registry 最終 Fresh Gate
- compileall PASS。
- Registry/collision/backprojection：87 passed。
- Registry 真 Tk GUI matrix：3 passed。
- Assembly 3D / dimensions / FinalScene / return-to-2D：38 passed。
- Project / receiving：29 passed；2 條 stale GUI test 經 082214 基準 fresh 比對為既有失敗。
- `自訂(9)`：CERTIFIED 38×27，Head/Tail、Save/Reload、canonical diff=0。
- `自訂(10)`：CERTIFIED 40×23 + 16×4，Head/Tail、Save/Reload、canonical diff=0。
- 最終封包時間戳：20260829_133925。

- 封包第一輪實解驗證：FULL 681/681、UPDATE 28/28 SHA256 全一致；0 missing / mismatch / extra；`config.ini` 未變。


## 2026-08-29 15:xx — INSERT 單級拓撲與 linked-FW C04 修正

- 純 `INSERT` 與 `OVERLAY` 是**單級截角**；資料模型現在會強制清除任何殘留 `secondary_retain_t` / `secondary_depth_t`。
- Certified Registry 增加 topology boundary validation：`INSERT` / `OVERLAY` 規則若回傳二級 geometry，直接拒絕，不得成為 canonical material。
- 撤銷錯誤的 linked-FW `INSERT_OVERLAY` 推導 `16×23 + 14×4`。既有 2026-08-21 C04 製造契約明確規定第二級 CUTTING = `side_fold + 0.5T`；第一級 X = `side_fold + FW`。
- 因此 T=2、side_fold=15、FW=25、無獨立 ytop1 的 linked-FW fixture 正確為 **`40×23 + 16×4`**。有 ytop1=16 的標準 C04 fixture 為 **`40×39 + 16×4`**。
- `ENDCAP_TOP_INSERT_OVERLAY_LINKED_FW_V1` 信任等級改為 `CERTIFIED`，證據來源改為既有 C04 製造規格，不再以錯誤 3D 候選升格。
- 實際 `自訂(10)` 切換為純 INSERT 後：Head/Tail = **38×27，secondary=None**；Save/Reload 後仍保持單級。

## [SUPERSEDED] 2026-08-29 15:xx — OVERLAY flat-X 名義側折中間修正
- 根因：`OVERLAY` 正確要求 X topology 為 flat、無左右 X BEND；但 EndCap builder 與 Certified Registry 又把此「有效折彎=0」直接拿去當截角公式的 `fold_u=0`，因此標準 `side=15/FW=25/ytop1=16/T2` 被錯算成 `25×39`。
- 正確契約：flat-X 只移除 BEND；截角仍使用原始 `yl1/yr1` 作名義側折基底。標準 OVERLAY 為 `primary_u=nominal_side+FW`、`primary_v=ytop1+FW-1T`，因此此 fixture = **40×39，單級**。
- Source of Truth 修正：`build_unknown_endcap_result()` 分離「實體 fold topology」與「relief nominal fold basis」；manufacturing request 保存 nominal fold；PartRenderData metadata 傳遞給 Certified Registry；solver rebuild 保留 metadata。
- TDD：新增 `tests/test_overlay_nominal_side_fold_contract.py`，先確認 2D builder 與 Registry 都 RED=25×39，再修成 GREEN=40×39。
- 真 GUI：Head/Tail 均命中 `ENDCAP_TOP_OVERLAY_STANDARD_V1`，40×39，secondary=None，2D vs assembly diff=0。
- 反向回歸：`自訂(9)` INSERT 仍 38×27；`自訂(10)` INSERT_OVERLAY 仍 40×23+16×4，兩者 Save/Reload metadata 一致。


## 2026-08-29 — UPDATE 驗收工件缺檔修正

- 發現既有交付規則「UPDATE 僅收錄相對基準的差異檔」存在安全漏洞：若使用者將較新的 UPDATE 套在未包含早期新增測試的舊工作目錄，可能出現交付日誌已聲稱新增 registry gate 測試，但本機實際沒有該測試檔。
- 實際核對：`tests/test_phase6_assembly_intent_registry_matrix.py` 與 `tests/test_phase6_assembly_registry_gui_matrix.py` 在 FULL 075946 之後皆存在；但 082214 / 152212 / 160848 等 UPDATE 因兩檔未再次修改而未重複收錄。
- 新增 `release_required_artifacts.json` 與 `tools/phase6_release.py`。新規則：UPDATE = 真實差異檔 + mandatory verification artifacts。
- mandatory artifacts 至少包含兩支 Assembly Registry Gate 測試、Joint registry matrix、Certified registry completion、release integrity gate 與 registry verification 文件。
- 打包前若 mandatory artifact 不存在，release collector 直接失敗，不得產出 UPDATE。
- 新增 `tests/test_release_integrity_gate.py`，驗證即使 mandatory gate 檔案與 baseline 完全相同，也必須被 UPDATE 收錄；缺任一 mandatory 檔則 fail closed。
- Fresh 執行：`test_phase6_assembly_intent_registry_matrix.py` = 18 passed；`test_phase6_assembly_registry_gui_matrix.py` = 3 passed；release integrity gate = 3 passed。

## [SUPERSEDED AS COMPLETE CONTRACT] 2026-08-29 22:xx — 金庫型 OVERLAY 下方截角 364→394 basis 修正
- 實檔 `金庫型貼外.p6fold`：W=400、T=2、nominal side=15、FW=25、下方 `CROSS + EXTRA_CUT + WIDTH + 1.5T`。
- 錯誤根因：flat-X 時把 nominal side=15 當成四角共用 relief X basis；下方被算成 `15+3=18`/側，剩寬 364。
- 正確語意：上方 OVERLAY 裝配截角仍用 nominal side basis；下方 CROSS 用 physical X bend basis。OVERLAY 無左右 X BEND，因此下方 basis=0。
- 修後：上方每側 40、剩寬 320；下方每側 3、剩寬 394。
- 2D / 單板3D / 組合圖 / Save→Reload 實檔材料完全一致，symmetric difference=0。
- 新增 Skill `.agents/skills/engineering/phase6-overlay-relief-basis/SKILL.md` 與 regression `tests/test_overlay_nominal_side_fold_contract.py`。

## [SUPERSEDED] 22:xx OVERLAY flat-X nominal-FW 中間更正
- 使用者實檔 `金庫型貼外.p6fold` 證明先前上方 `40/320` 契約錯誤。
- 正確：W400/T2/FW25 時，上方每側 U CUT=FW=25、單側未截長375，`25+375=400`；左右都截後中央350。下方 1.5T 每側3、中央394。
- 根因：legacy nominal yl1/yr1=15 被錯用為 flat-X manufacturing X fold basis。
- `sheetmetal_part_adapters` 與 Certified Registry flat-X basis 統一為0；nominal yl1/yr1僅保留 metadata。
- Skill / CONTEXT / AI_HANDOFF / SOP / pitfall / verification 同步撤銷 40/320 錯規則。


## 2026-08-29 23:xx — OVERLAY formed FW Registry / 3D Relief 正式化
- 使用者再次確認物理驗收：`FW成型29你不截29怎麼插進去？`。因此前兩個中間答案 40/320 與 25/350 都撤銷。
- 正式區分：EndCap nominal/material FW=25；Box Body Fold Profile 折後 formed FW occupation=29。上方 OVERLAY X CUT 必須避讓 formed occupation，不能只看 EndCap nominal FW。
- `金庫型貼外.p6fold` 現行真值：上方每側 29、中央342、單側 `29+371=W400`；V 仍由 EndCap FW25 算 39。下方每側3、中央394。
- Certified Registry：`ENDCAP_TOP_OVERLAY_STANDARD_V1` 升 revision 2，`geometry_inputs` 包含 `BOX_BODY_FORMED_FW`，X formula=`primary_u=mating_width`；不得硬寫29。
- Registry evaluator 直接共用 `phase6_fold_profiles.formed_box_body_fw_widths()`；UI/2D/3D 不得各自重算 formed FW。
- 3D solver certified shadow 保存 geometry_inputs / formed_fw_by_corner evidence；Head/Tail post-solve residual pair count=0；`ResolvedReliefRuleTrace` 同步保存 revision/evidence。
- persistence 升 `relief_contract_version=2`，source 保存 formed-FW fingerprint 與 per-part registry rule revision。原始實檔 versionless/revision-1 的 40-mm committed relief fresh load 即失效，不再重播。
- 真實 fixture `tests/fixtures/vault_overlay_w400_t2_fw25.p6fold` 為使用者上傳檔的 byte-identical copy。
- 回歸：affected core 165 passed；INSERT/OVERLAY/INSERT_OVERLAY GUI matrix 3 passed；實檔 2D/single3D/assembly/shared dimensions/Tail/Save-Reload 81 passed。
- 現行 Skill / AI_HANDOFF / CONTEXT / SOP / pitfall / verification 已統一為 formed-FW Source of Truth。
