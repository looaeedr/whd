# 2026-09-07

## Receiving Door FW semantic correction

- Root cause: Receiving `FW=29` is already the formed outside occupation, but the shared Door engine treated it as material FW and added `2T` again, producing a false 33 mm frame occupation at `T=2`.
- Fix: Cabinet Family policy converts Receiving Door FW from formed occupation to material space before entering shared Door geometry. Vault / 金庫型 keeps material `FW=25`, so its existing `25+2T=29` behavior is unchanged.
- Unified consumers: outer Door manufacturing/render/export, GUI legacy Door seams, Receiving inner-door derivation, and assembly placement now consume the same family-aware FW semantic.
- Correct default Receiving results:
  - upper Door 800×1100 -> finished 735×1064 -> blank 769×1098
  - lower Door 800×500 -> finished 735×435 -> blank 769×469
- TDD RED evidence: GitHub Actions run `34066578746` at `cc230fd68a568b49ff0e29fd969a4a750ea38274`: 30 passed / 3 failed / 4 skipped; all three failures were the expected old Receiving 2T double-compensation values.

- GREEN evidence: GitHub Actions run `34067119430` at `966b79f01916093978434054038844d8a431959c`: **38 passed / 7 skipped / 0 failed**.
- `config.ini` SHA256 before/after QA remained `980eab68d4a1732a5313b22329852dfc9691c83e4e2a64cccd18022afae4ee67`.
- 3D evidence artifact `9999289925` (`door_fw_3d_check.png`) was generated from authoritative outer/inner Door placement and manually inspected; inner panel is smaller and inward from the corrected outer Door plane as expected.
- One-shot QA workflow was removed after terminal GREEN; cleanup commit `a4b10dfc8191524ca00b93777c07540d35ed1412`.


## Receiving Door projection/snapshot single-source follow-up

- Root cause follow-up: Fold Designer still had two independent Door dimension calculations, and main GUI snapshot creation had a third. All accepted operator `FW=29` as if it were material FW and therefore re-created the false 33 mm frame occupation.
- RED run `34083065751`: 1 passed / 2 failed. Receiving upper/lower projections were 727×1060 and 727×427 instead of 735×1064 and 735×435; Vault guard passed.
- Fix: `fold_designer_bridge.py` and `gui.py` now resolve family-aware material FW and call the shared Door finished-size resolver rather than hand-writing `FW+2T`.
- GREEN run `34083409925` at `38dd8a4b2d76df82d7e7830503be53beaae506e0`: focused projection 3 passed; T25 Combined Acceptance 1 passed.
- `config.ini` before/after: `980eab68d4a1732a5313b22329852dfc9691c83e4e2a64cccd18022afae4ee67`.
- Production scan found no remaining caller-level Door `fw+2T` formula in `gui.py` or `fold_designer_bridge.py`; the remaining `frame_span = fw + 2T` is the generic material-space engine contract.
