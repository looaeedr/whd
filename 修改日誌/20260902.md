# 2026-09-02 修改日誌

## Phase6 Knowledge Preflight：補齊踩坑庫硬閘門

- `AGENTS.md` 第 0 節由 Skill-only Preflight 升級為 Knowledge Preflight。
- 全域踩坑庫 `個人AI檔案庫/第二層_專案與SOP/06_踩坑記錄與防錯經驗庫.md` 改為所有任務永遠必讀。
- `.agents/skills/skill_registry.json` 的組合/截角與 OVERLAY route 自動加入 `個人AI檔案庫/踩坑庫/phase6_assembly_relief_pitfalls.md`。
- `tools/phase6_skill_preflight.py` 新增 `REQUIRED REFERENCES` gate；reference evidence 必須使用 `READ_REFERENCE: <repo-relative-path>`，檔案不存在或 evidence 缺失均不得通過。
- `tests/test_phase6_skill_preflight_gate.py` 增加 fail-closed 回歸，防止後續再退化成只驗 Skills。
- 修改前已備份原檔至 `BACKUP/20260902-060301/`；`config.ini` 未修改。

## Release QA 可續跑測試 Runner
- 新增 `tools/phase6_release_test_runner.py`：pytest collection 綁定 SHA256/count、JSONL journal、state JSON、時間預算 `exit 75`、timeout process-group kill、timeout/failure 自動二分縮批。
- 新增 `tests/test_phase6_release_test_runner.py`，覆蓋 resume、collection drift、kill process tree、journal 重啟、release policy/Skill/踩坑庫契約。
- `release_required_artifacts.json` 將 runner 與測試列為 mandatory update files。
- `phase6-release-packaging/SKILL.md` 正式禁止人工靠對話記憶切長回歸；Headless/Xvfb 各用獨立 journal，重複執行到 exit 0。
- Runner 新增 `complete_teardown_timeout`：pytest summary 已完整 PASS 但 GUI/interpreter 收尾不退出時，保留 teardown 證據、kill process group，並視為完成，不再卡住 release gate。


## T11 可續跑 Release Runner hard-kill／live-canonical 實檔回歸修復
- `tools/phase6_release_test_runner.py`：Linux 自管 Xvfb 新增 parent-death signal，防止 runner 被外層 `SIGKILL` 時留下 orphan Xvfb；真 hard-kill regression 已先 RED 再 GREEN。
- `.agents/skills/engineering/派工/SKILL.md`：合併 17:20 TIMEOUT/journal/process-group 協定，並補上 outer hard-kill / `PDEATHSIG` 硬規則。
- `release_required_artifacts.json`：派工 Skill 與其 timeout contract test 列入 mandatory update files，避免 cumulative checkpoint 文件宣稱包含但 ZIP 實際漏檔。
- `tests/test_phase6_linked_fold_chain_and_parts.py`：實檔 remove/add 與 2D depth 改驗 live-canonical/正式 Return-to-2D，不再呼叫已取消的 Confirm transaction。
- `tests/test_phase6_user_p6fold_live_sync.py`：`自訂(6).p6fold` 舊 INSERT relief 改驗被目前 `ENDCAP_TOP_INSERT_OVERLAY_LINKED_FW_V1@1` CERTIFIED 解原子替換（40×23 + 16×4），不再錯誤期待 disabled。
- Fresh focused Xvfb：3/3 passed；兩個真 `.p6fold` 模組合跑：43/43 passed；runner + dispatch contracts：19/19 passed（文件同步前證據）。
- `config.ini` 未修改。

## 受電箱 800×1600×350 family transaction／3D WRAP ownership 修正
- 根因 1：`fold_designer_bridge._phase6_store_editor_values()` 把所有非 bool setting 都做 `float()`；family snapshot 同時帶有 `ui_text_size="medium"` 時拋 `ValueError`，又被 family switch 外層 `except Exception: pass` 吞掉，造成 snapshot 已 800/1600/350、live settings / 3D 輸入 / 主 GUI 仍停在舊值。
- 修正 1：`ui_text_size` 改走 `normalize_ui_text_size()`，snapshot 與 settings 使用同一 normalized value；新增 GUI red/green regression，驗 snapshot、`_settings_values`、3D W/H/D、主 GUI 全部原子更新成 800/1600/350。
- 根因 2：Receiving family switch 直接把 Head/Tail BOTTOM 寫成 WRAP，3D 又額外建立「啟用外側包覆」常駐 Checkbutton 與「啟用 WRAP」解鎖 Checkbutton，讓 Cabinet Family／legacy UI mirror 與 Assembly Intent/Joint Graph 同時擁有 relation。
- 修正 2：受電箱 family 只提供 geometry/reserve defaults，不再自動製造 BOTTOM WRAP；正式 `包覆貼外` preset 才由 Intent → Joint Graph 投影 BOTTOM=WRAP。移除 3D 獨立 enable toggle；只有 canonical BOTTOM=WRAP 時，解鎖頁顯示 X/Y 預留，且預留 editor 不可改寫 Joint relation。
- Fresh regression：直接受影響測試 34/34 passed；Joint Graph / Intent / Receiving Registry ownership 53/53 passed。
- `config.ini` SHA256：`e0d8e0c9a6db736f1f7882ff2246cb2845467431bafa583f81a35a0e1d551dc5`，本輪未修改。



## 移除已失效的外部使用者 `.p6fold` 永久測試
- 使用者確認已不再持有 `/mnt/data/自訂.p6fold`、`自訂(2).p6fold`、`自訂(6).p6fold`；正式 suite 不再保留對這三個 ephemeral 路徑的 conditional skip。
- `tests/test_phase6_linked_fold_chain_and_parts.py` 移除 6 顆依賴上述外部實檔的測試；`tests/test_phase6_user_p6fold_live_sync.py`（3 顆）整檔移除。
- 新增 release policy regression，禁止 `tests/**/*.py` 再硬編碼這三個 retired absolute paths。
- `release_required_artifacts.json:update_cleanup_paths` 加入 `tests/test_phase6_user_p6fold_live_sync.py`，確保從舊 T11 基準套 UPDATE 時會移除舊測試檔。
- collection 由 1291 顆變成 1284 顆：移除 9 顆外部實檔測試，新增 2 顆 policy guard（禁止 retired 絕對路徑、要求 release skill 明文禁止 ephemeral fixture 依賴）。
