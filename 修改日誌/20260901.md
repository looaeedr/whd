# 2026-09-01 修改日誌

## Joint Graph / STANDARD / Certified Relief 收斂

- 新增 Assembly Intent Registry 與 explicit four-edge Joint Map。
- Joint serialization 升級 v2；補 schema-1 side-only legacy migration，graph-first 保留 user/explicit overrides。
- UI / Fold Designer 拆開 preset mirror 與 resolved graph ownership，移除正常流程 Top CornerType 反推覆寫。
- Corner resolver 改讀 nearby AssemblyJoint；Certified Registry 支援 edge-aware signature、2-stage OVERLAY、完整 rule metadata/diagnostics。
- OVERLAY runtime 正式收斂到 `ENDCAP_TOP_OVERLAY_STANDARD_V1@3`：STANDARD `40×41` → `40×39 + 15×2`。formed FW=29 只作 shadow/historical evidence。
- blank/piece dimensions 改讀 material segment chain / physical piece topology，不讀 relief 後 polygon bbox 當採購/展開 W×H。
- Receiving BOTTOM WRAP 改由 Joint Graph/Registry 擁有，Family 只供 geometry/reserve；修正舊 family-owned bottom policy。
- replay fingerprint 納入 Joint mechanical semantics、family structure、registry revision，修正 provenance/legacy region alias 導致合法 canonical cut 被誤判 stale。
- diagnostics 改為 joint-local；BOTTOM Certified trace 不再串到 TOP。
- 修正 Indicator Box → direct door-indicator 切換後 stale workspace presence 汙染 Save/Reload。
- 修正 part switch outgoing idle callback 導致重複 redraw、以及 shared corner measurement formatter NameError。

## T8 測試與文件遷移

- 將多個 legacy side-only intent、Top CornerType ownership、首頁/button UI、confirm/cancel、formed-FW 29、OVERLAY single-stage、Receiving bottom INSERT_OVERLAY 等歷史 oracle 改為目前正式契約。
- 外部 `/mnt/data/自訂*.p6fold` 缺失時只 skip 明確依賴該實檔的 cases。
- 更新 `phase6-overlay-relief-basis` Skill，舊 2026-08-29 formed-FW 文件標示為 superseded historical evidence。
- 新增 Joint Graph migration verification，並更新 release mandatory artifacts。

## 保護項

`config.ini` 未修改；正式 FULL/UPDATE 仍須依 `phase6-release-packaging` 完成 canonical baseline、CRC/SHA/Unicode、fresh extraction 與 packaged FULL regression gate。

## Final QA / Packaging

- 完成 #126～#166 fresh module QA；修正數個 stale test oracle（Hole Editor layout owner、schema-driven settings hidden keys、3D Menubutton UI、繁中板厚錯誤訊息），production 不倒退舊 ownership。
- 完成 Registry/Joint/WRAP/3D/blank/Save-Reload 高風險 release matrix。
- canonical baseline 經 CRC、root layout、config SHA 驗證；UPDATE 覆蓋 + cleanup 後與目前 source 逐檔 SHA 完全一致（missing/mismatch/extra = 0/0/0）。
- 完成 packaged FULL fresh extraction 功能 gate；正式封包以最後一次 Asia/Taipei timestamp 重建。
### [19:44:50] 變更紀錄
- **變更檔案**：$_, $_, $_, $_, $_, $_, $_, $_, $_
- **備份檔名**：$_, $_, $_, $_, $_, $_, $_, $_, $_
- **修改摘要**：清理 基準檔 內錯置的 Python 程式與測試副本；正式程式入口仍使用專案根目錄 gui.py、old_designer_bridge.py、phase6_fold_profiles.py 與根目錄 e_engine package，並保留 基準檔\gui.py 相容啟動器。

### [19:51:08] 變更紀錄
- **變更檔案**：.agents/skills/engineering/派工/SKILL.md, docs/superpowers/verification/2026-09-01-dispatching-skill-frontmatter-verification.md
- **備份檔名**：BACKUP/20260901-194916-SKILL-派工.md, BACKUP/20260901-195108-20260901.md
- **修改摘要**：補上派工 skill 的標準 frontmatter，設定 
ame: dispatching、人工指定用 description 與 disable-model-invocation: true；新增 verification evidence，並確認本地 41 個 SKILL.md 已無 missing name、重覆 name 或內容重覆。


### [Receiving EndCap D-2T 回歸修正]
- 修正 `ae_engine/cabinet_types/receiving.py:endcap_depth_comp_t()` 被誤改成 `3T` 的回歸；受電箱封頭／封尾 D 材料核心恢復 canonical `D - 2T`，金庫型仍維持既有 `D - 3T`。
- `gui.py:_end_cap_part_spec_from_values()` 不再以 `3T` 作所有 Family 的隱含預設；caller 未明傳時會依 `model_name` 取得 Receiving Family policy，堵住靜態/重建路徑回落 Vault 公式。
- 更新 Receiving 2D/Profile/Save-Reload 測試 oracle：D350/T2 的 EndCap D material core = 346；補 GUI adapter family-policy regression。
- 清理 `PROGRESS_README_20260831.md` 殘留的 Receiving `D-3T` 敘述，避免後續維護再次誤套金庫型公式。

### [Receiving EndCap Y Fold Chain canonical topology 修正]
- 根因：`phase6_fold_profiles.py` 只把金庫型 9 段 `zl1/zl2/FW/D/W/D/FW/zr2/zr1` 視為 canonical；受電箱 Family 正規 transform 移除 `zr1` 後成為 8 段，卻被誤判為 user-custom topology。
- 錯誤 generic mating mapper 因此把箱身 `zl1`、`zl2` 各自複製為 EndCap Y 的 `box_mating:zl1`、`box_mating:zl2`，造成封頭／封尾 Y 軸多一折，並使 Certified Registry 誤走 `ytop1_absent` 的 linked-FW 規則，出現 `40×23 + 16×4`。
- 修正 canonical 判定改為比對「目前 Cabinet Family 自己的 `build_box_body_profile(snapshot)` topology signature」；忽略尺寸值，只比對 segment key/core/BEND ownership/angle。金庫型 9 段與受電箱 8 段因此各自辨識正確，真正增刪折／改角度的自訂 topology 仍走 generic mapper。
- 受電箱 canonical Head Y 恢復 `ytop1 → FW → D-core → ybottom1`；Tail native Y 恢復 `ybottom1 → D-core → FW → ytop1`。不再生成 `box_mating:zl1/zl2`。
- 上方 `INSERT_OVERLAY` 因 `ytop1` 正確存在，Certified Registry 改命中 `ENDCAP_TOP_INSERT_OVERLAY_STANDARD_V1`；T=2、side fold=15、material FW=25、ytop1=16 時恢復 `40×39 + 16×4`，不再誤用 linked-FW `40×23 + 16×4`。
- 新增 Receiving family regression，並保留既有五段 `FW-D-W-D-FW` 的合法 `ytop1_absent` 行為與任意自訂多段 mating chain 行為。
