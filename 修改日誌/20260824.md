## 07:01 基準型號單一選單實機驗證與累積更新修正

- 針對使用者回報「畫面一樣、仍看不到受電箱」重新以真實 Tk widget tree 驗證，不再只檢查原始碼。
- 主 GUI 實際建立後只允許一個「基準型號」Combobox；不得建立第二個「盤體類型」欄位。
- 當既有基準清單含金庫型時，同一個下拉選單必須顯示 `金庫型 / 受電箱 / 自訂`。
- 加入本版可視執行識別碼 `RECEIVING_MODEL_SINGLE_SOURCE_20260824_0701` 到主視窗標題，避免新舊執行路徑混淆。
- 修正交付基準：UPDATE 必須相對使用者原始 `PHASE6_FW_LINK_BUGFIX_FULL_20260823_212355(3).zip` 產生累積更新，不得只相對前一個 ChatGPT 更新包。

# 2026-08-24 修改日誌

## 受電箱基準型號／盤體類型單一 Source of Truth 修正

- 修正上一版錯誤新增「盤體類型」選單：使用者介面只保留既有「基準型號」單一選擇，不再維護第二份 `cabinet_type_var`。
- `受電箱` 直接加入既有「基準型號」選單；主 GUI 與 3D 全域設定使用同一份 model 清單。
- 受電箱第一階段沒有自己的 baseline DXF 也仍可列為公式型基準型號；不會因此強迫沒有實體基準檔的 `金庫型` 出現在清單或搶走原本 `自訂` 預設。
- 新專案只保存 `model`；不再序列化獨立 `cabinet_type`。短暫錯版若已保存 `cabinet_type=受電箱`，載入時會自動遷移為 `model=受電箱`。
- 3D 內直接將基準型號切到受電箱時，會同步套用受電箱 W/D/FW、門四折、8 段箱身 Fold Chain 與固定側背分離 policy，不只更換選單文字。
- 新增回歸測試鎖定：主 GUI 無第二個盤體類型 selector、主/3D 基準型號都看得到受電箱、單一 model 存讀、舊錯版遷移、3D 切換實際套用受電箱 policy。

## 受電箱第一階段 2D

- 新增受電箱 Cabinet Family 第一階段 2D policy，沿用既有共用幾何核心，不新增第二套引擎。
- 箱身固定使用側背分離，主 Fold Chain 移除 `zr1`，預設值為 `-24 24 29 350 800 350 29 18`。
- 封頭／封尾 `ybottom1` 以後面板 W 外側接合為準，D 向核心採 `D - 2T`。
- 封頭／封尾下方使用既有 `INSERT_OVERLAY`，下方等價 FW 為 `side_rear_bend + 1T`，預設參數 `0.5T / 0.5T / 2T` 並保持可調。
- 門四邊折預設改為 `19 / 19 / 19 / 19`；門縫 3.5 mm 與 C02 沿用既有規則。
- Family 切換會保留各自 runtime 設定；專案儲存／重載會保留受電箱 Family、側背分離與 linked EndCap 的 `D - 2T` 語意。
- Fold Designer 的已知型號截角重設與 Final Part policy 會辨識受電箱，不會把下方角退回金庫型規則。

## 驗證

- 新增 `tests/test_receiving_cabinet_2d.py`，涵蓋 Family defaults、箱身 8 段 Fold Chain、側背分離、封頭尾 D 補償、下方嵌入貼外、門預設與專案重載。
- 完整測試基線中的 4 個失敗皆為附件缺少 `/mnt/data/自訂.p6fold`；與本次程式變更無關。
- `config.ini` SHA-256 在修改前後保持一致，未被修改。

### [19:16:29] 變更紀錄
- **變更檔案**：	ests/test_phase6_box_body_audit_contract.py`n- **備份檔名**：BACKUP/20260824-191519-test_phase6_box_body_audit_contract.py`n- **修改摘要**：修正測試模組內部引用路徑，避免 pytest collection error。


### [19:19:35] 變更紀錄
- **變更檔案**：old_designer_bridge.py, phase6_settings_panel.py, DELIVERY_README.md`n- **備份檔名**：BACKUP/20260824-190551-fold_designer_bridge.py, BACKUP/20260824-190551-phase6_settings_panel.py`n- **修改摘要**：套用 PHASE6_3D_UI_RELAYOUT_UPDATE_20260824_005731 更新，恢復 3D UI 置頂區與 Menubutton 選單位子佈局。

